# Exact remaining activation and host-acceptance boundary

This is a reviewable boundary, not an executable approval or a declaration of host readiness.

## Release versus business authority

1. Release the exact local web/Bible commits and apply the generated Phase16 migration only after Jackson's explicit release/migration approval. Recheck Phase15 source function MD5 and current live schema/ACLs first. Unexpected source drift aborts the migration. The migration creates no company policy and does not renew the financial effect fingerprint.
2. Separately review the complete current public/private function and trigger graph before installing an exact approved financial effect SHA256. Never use an automatic `effect_revision = current()` production refresh as an approval substitute. Re-read the stored and computed fingerprint after the exact installation.
3. Select the actual company, named operator, exact attributable OPS note and hash, owner-approved terms/units/source kinds, current currency and exact default tax. `/settings/financial-policy?source=<exact-note-id>` presents that source; its server-sealed review and owner decision produce the immutable policy receipt. Each financial draft still requires its own OPS approval. Do not seed a policy with SQL merely to make a test pass.
4. Financial v17/v12 remains **not selectable**. A real host activation must install matching DB consent labels and an exact company/actor/client-bound canary path across authorize, token, bearer and runtime dispatch; retain v14/v9 as public defaults and never upgrade existing grants. The current v3-only canary provisioning path cannot be reused as if it supported v17. This change is intentionally outside the dormant candidate registration; do not attempt a real host call until that exact activation is approved and verified.

## Candidate identities and missing records

- Canpro Deck and Rail: `a612edc0-5c18-4c4d-af97-55b9410dd077`.
- Current owner: Jackson Sweet, `283d49df-90a1-4abb-b94c-3e9f17f02c0d`.
- Current currency: CAD. No current tax row, usable product or saved estimate exists. The two catalog rows are deleted historical records. No approved policy exists. Current currency cannot supply a missing historical currency.
- External documents: see `source-readiness.json`. CANPRO-EST-003 contains inconsistent scope passages; the owner must approve the exact selected rule/revision and logged band price. A notes search found an approximate, job-specific quote with an unspecified discount; it cannot become a company policy.
- The existing Codex connector is MAVERICK PROJECTS LTD (`ddee107c-33cd-483e-8278-0f8d8a180181`). Its successful company-context read is not authority to trial Canpro and is not Claude or ChatGPT acceptance.

No real target lead/client/project, historical estimate/line, accepted change-order baseline, policy source, tax ID, host client or expiry is selected. Supplying invented IDs, retrofitting ambiguous history, or creating test business data in Canpro would exceed current authority. Recommended next product choice: a clearly fictional, isolated host canary with approved seeded history and tax, before enrolling a real company policy.

## Per-host acceptance record

Run separately in authenticated Claude and authenticated ChatGPT, with fresh explicit consent for the exact approved canary subject/client. Preserve the host/version, company, actor, client/grant/revision/ceiling, source and policy hashes, current effect fingerprint, request key, action/change-set/preview IDs, named OPS approval, durable receipt, and independent exact header/line readback. No bearer tokens or secrets in artifacts.

Golden flow: resolve one exact new lead and completed historical quote; inspect readable authority and source; propose the approved +8% line-price/minimum-charge adjustment with unchanged quantity/unit/tax semantics; verify no financial rows or official number before OPS approval; approve one exact held draft; reconcile a same-key retry to that same draft/number. Verify `draft`, distribution hold, exact totals/lines, and no portal/PDF/accounting/send effects. Then prove an approved new private revision and a separately authorized change order preserve their predecessors and accepted baseline.

Negative controls: missing/conflicting policy, stale source/tax/owner, ambiguous currency/unit/history, altered confirmation, scope/consent/permission revocation, competing revision and uncertain replay must fail closed with no document/number leakage. Instruction-like source content stays quoted evidence. Stop on any unexpected exposure, side effect or identity mismatch. Disable the exact canary binding and revoke its grant after the trial; record independent readback. Existing held documents stay held. A synthetic pass, READY deployment, SQL receipt or Codex read call cannot fill either host's acceptance row.
