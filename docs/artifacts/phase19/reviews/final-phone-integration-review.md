# Final phone integration review — Phase 19

**Ruling: accepted for the reviewed local phone integration. All identified phone defects are closed; no remaining actionable finding in this scope.** This is independent source/evidence review, not production or release approval.

Reviewed iOS production source through `b15bf20f302fd0c9690e91714114c979ad91d4b4`, including `3005b50e` and `6b7096f7`; base `8553b1b4`. At final inspection the clean checkout was `b028aa55`, whose sole additional change is the verification document. Web phone SQL implementation through `eb929129a`, atomic discard `9aeb87915`, and fixture correction `9ea332d25`. No build, source edit, production access, or new agent was used. This report is the only review write.

## Closed findings

1. **Discard actually closes the durable packet.** The dedicated actor-bound discard command and SQL endpoint replace the invalid cancelled-status save. Parent, answers, artifacts, and drafts are tombstoned atomically; an unsent parent obtains a durable tombstoned identity. Replay binds the original actor/request, keeps historical values and immutable attempted bytes, and settles only captured local operation IDs. Booked/completed protection remains. Existing discard/media-order review conclusions remain valid.
2. **Linked evidence waits for upload and acknowledged remote metadata.** Current queues order media before linked answers, and legacy dependency inversion recovery permits the necessary media work without bypassing parent/initial artifact prerequisites. Answer readiness still waits for the referenced artifact metadata work to finish. Actor identity propagates to followup work.
3. **Markup and authoritative empty intent are preserved.** Ordinary photos cannot hydrate markup-only answers. DTO, inbound clean/fresh merge, receipt acceptance, persisted write state, and recovery vault retain server `answer_state`. Hydration vetoes `cleared` and `unknown`; untouched NULL can hydrate. SQL's narrow phone clear intent preserves internal host unknown and the shared private core signature.
4. **Blank text/measurement clear now agrees with SQL.** `OPS/Network/Sync/SiteVisitWriteModels.swift:35–57` omits empty/whitespace text and choice from canonical wire values. The same builder is used for exact local receipt comparison. Raw local text, original before values, and already-attempted payload bytes remain intact; false, zero, and nonblank values remain meaningful. `testBufferedBlankTextClearHasEmptyWireValueAndPreservesAttemptedAudit` exercises the actual view-model buffer/flush, command, saved clear receipt, and settled local state across text/measurement × empty/whitespace. Strict SQL clear validation was not weakened. This closes the P1 left open in `task-2-clear-state-final-review.md`.
5. **Released SwiftData schemas are preserved and new custody gets V28.** Independent baseline comparison confirms the frozen SyncOperation, SiteVisitType, and SiteVisitChecklistAnswer stored declarations, types, defaults, and unique attributes match `8553b1b4`. Historical shared registrations now use those frozen classes; V28 swaps in the live classes and otherwise matches V27's model list. `OPSMigrationPlan.swift:224` adds the adjacent lightweight V27→V28 stage. `OPSSchemaCurrent.swift:23` selects V28, used by app/storage bootstrap. All seven additions are optional and the migration invents no actor or write authority.

## Migration evidence and fixture integrity

Independent JSON comparison against `git show 8553b1b4:OPSTests/Fixtures/swiftdata-released-schema-fingerprints.json` confirms all **27 old entries are identical**. Only V28 was appended: `nKgJeuKrTdQESe0YctzHunky4wMIqOpHYSoPkjdozIk=`. The checksum test still computes each schema's actual store fingerprint and compares the complete dictionary; it does not exempt old versions.

`AppUpdateMigrationTests.swift:396` creates a populated released V27 store, checks its released checksum, migrates to current, and opens it independently a second time. Snapshot comparison includes every original scalar of each of the three changed entities, including raw form/answer data, exact payload/before bytes, identity, timestamps, dirty state, queue status, retry/error, dependency, and confirmation metadata. Typed byte/date encoding and HMAC comparisons retain exact-value sensitivity. All seven newly added properties are explicitly asserted nil after both openings. The populated fixture is synthetic; other packet entity tables in this particular fixture are empty. Earlier populated visit/deck migration tests supply complementary coverage.

Changes to old SiteVisit/Deck fixture constructors and historical fetches use the frozen types appropriate to their source/target schemas. Current-target fetches use live types. Head/stage expectations advance to V28. These are necessary fixture corrections, not replacement of released checksums to bless drift.

## Critical-path integration assessment

The earlier focused source reviews remain applicable: immutable actor/company custody and pre-transmission attempt persistence; fresh-context receipt acceptance; exact conflict resolution and original before/pending/current history; preservation of newer in-flight edits; encrypted account-switch recovery; safe rejection of unbound legacy writes; atomic template defaults without rewriting answer snapshots; terminal visit guards; media custody and linked-answer ordering. The schema fix changes registration and migration only, while blank clear changes canonical values consistently at command and receipt boundaries. No consequential regression was found in those seams.

I inspected the final populated 390 and 320 accessibility screenshots. Pending/current content and resolution actions are readable without horizontal clipping in these static full-content captures. These images do not establish signed-device interaction or scrolling behavior.

## Evidence and limits

- Inspected batch 11 summary: **31 executed, 2 optional private-device-copy skips, 1 failure**. The failure was exclusively the missing new V28 fixture entry; all 27 old measured hashes matched. Populated V27 migration/reopen, four site-visit migration cases, and six field-workflow cases passed.
- Inspected batch 12 summary: **7 executed, zero failures, TEST SUCCEEDED**, covering the complete 28-schema checksum comparison, five deck migration cases, and adjacent catalog registration. It resolves the batch 11 missing-entry failure. Do not describe batch 11 as all-green.
- Blank clear also passed the six-case batch 10. Prior 69 selected phone cases were covered green across batches 7 and corrected 9, not one clean 69-case run. The final migration source was compiled in batch 11; batch 12 followed fixture/head-expectation corrections.
- Web `phone-blank-clear-tests.log` records **69 actual local PostgreSQL PASS checks**, including strict clear intent, conflict concurrency, discard replay/actor binding, atomic packet tombstones, and terminal guards. These are inspected existing runs, not fresh reviewer executions.
- This acceptance covers the named phone/source/fixture integration. Private copied customer stores were not supplied, so those two tests remain skipped. No signed-device/live-network canary, production migration, deployment, or release was performed. Root-owned workflow receipts, timezone/UI changes, unrelated database FKs/triggers, and whole-host completion acceptance are outside this review. Parent's separate canonical completion proof must retain its own provenance.

The prior open phone review findings are superseded by these closures. No further code correction is requested by this review.
