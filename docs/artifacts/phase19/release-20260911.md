# P19 controlled release — 2026-09-11

Status: all six production migrations applied successfully; web rollout and signed phone build are in progress. Jackson approved continuing the web/database rollout and a compatible iPhone test build. This is not company/MCP write activation or an App Store release.

## Exact source custody

- Verified production alias `app.opsapp.co`: deployment `dpl_JBak4gpTm8yyfbPgewH9Cup7gY1z`, READY, source `006b69a8a35a3490efad229a09dc2473251a5c46`.
- Release source `9ee056620d6777a0c32c72f2c5a59d0228e9de14` descends from that exact production source. It contains the accepted P19 integration patch relative to its current-main parent, followed by the independently reviewed MCP display-title fix. P19 and MCP source paths compare byte-identically to reviewed local main `43edeee37f088e90c86c24de38cd17bf81099103`.
- Unrelated unpublished sidebar, command-palette and Phase C changes remain on local main and are excluded from this release. Primary shared checkouts are not switched, staged, stashed or restored.
- Compatible phone source `3f5eaffebd3d81e22d9165140f3c002bc4d22edd` includes V28 plus the independently verified suspension/input fixes. A signed Release/device build runs in the existing P19 isolated checkout; no installation, upload or App Store release is implied.

## Gates

1. Refresh production schema, function bodies, privileges, migration ledger and existing policy seals. Compare the six SQL hashes with the accepted handoff and Bible mirrors. Independent migration review must clear current dependencies and default-off behavior.
2. Run the exact release's focused tests and TypeScript checks. Verify no public V22/V17 selection or host commit authority is added.
3. Apply six reviewed migrations, one at a time in filename order, through Supabase migration tooling. Record actual ledger versions and independently verify nullable additions, RPC ACLs, RLS, untouched policy seals and empty compatibility/activation tables. No enrollment, reseal, grant/consent mutation, provider action or business-record test write is authorized by this rollout.
4. Push this exact release to production main without force, preserving the verified production ancestor. Verify Vercel READY, source SHA, production alias, auth challenge and any available signed-in read-only host checks.
5. Independently inspect the signed phone build and document distribution/device-proof limits. Update the Bible's release state and preserve all existing unrelated local work.

## Fresh proof

- 115/115 site-visit workflow/read/form/approval/civil-time tests, nine files, zero skips.
- 606/606 protocol/runtime/rate/UI/display/OAuth tests, thirteen files, zero skips. Log: `release-tests-20260911.log`.
- Focused production TypeScript exit 0. Log: `release-typecheck-20260911.log`.
- Focused protocol TypeScript exit 0. Log: `release-protocol-typecheck-20260911.log`.
- Independent release-source review: all 123 changed paths match the two selected commits; all 66 non-document paths are byte-identical to reviewed local main. No actionable findings.
- Signed-in native Codex company-context read succeeded before release. The current connection cannot list site visits because it lacks the required schedule/site-visit scopes; no grant was expanded or bypassed.
- Supabase changelog was refreshed on this date; no applicable breaking SQL migration/API change was identified. No extension pinning, realtime-schema mutation or analytics-log API use is involved.
- Existing infrastructure only; no subscription, paid-tier or API-credit change. Production invocation costs are not measured by these checks.

## Known pre-existing limits

Customer-update, financial and catalog effect seals were already stale before P19. Preserve their fail-closed behavior; this release does not silently reseal or claim those write trials are available. Native-host site-visit prepare/save acceptance and signed-device offline/recovery canaries remain separate from automated proof.

## Production migration ledger — 2026-09-12 UTC / September 11 local

| Source timestamp | Actual ledger version | Migration |
|---|---|---|
| 20260910190020 | 20260912003437 | site_visit_concurrency_protocol |
| 20260910191043 | 20260912003509 | site_visit_durable_writes |
| 20260910204942 | 20260912003525 | site_visit_canonical_actor_booking |
| 20260910205717 | 20260912003615 | agent_site_visit_workflow |
| 20260911010000 | 20260912003639 | site_visit_workflow_rate_limit |
| 20260911011000 | 20260912003656 | site_visit_packet_discard |

Each application confirmed success. Immediately before and after every application, the three existing stored effect policies remained unchanged, financial/catalog enabled unexpired trials stayed zero, and V22 candidate clients/grants stayed zero. Post-install company compatibility rows, effect-policy rows, proposals, corrections, phone receipts and discard receipts are all zero. The initial verification-output parser rejected a read-only result before any migration call; it was corrected before application. No SQL source was altered.
