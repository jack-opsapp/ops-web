# Approved catalog trial release

Required Skills: custom-skills:writing-plans, custom-skills:executing-plans, superpowers:using-git-worktrees, superpowers:test-driven-development, supabase:supabase, vercel:deployments-cicd, superpowers:verification-before-completion.

Jackson approved deployment, database migration and a temporary MAVERICK-only trial. Every business save still requires his exact approval inside OPS. Public exposure remains V14/V9. Existing financial bindings, policies, grants and business records are out of scope.

1. Test first: exact six-tool V19 trial, seven-scope ceiling, V14 consent, current binding, expiry, wrong subject, no public fallback, no commit tools.
2. Add a separate immutable catalog trial binding and service-only provisioning. Extend existing OAuth read/mint/refresh/revocation paths without changing legacy authority. Fence all catalog mutations to the original exact MCP trial context; OPS approval uses that same sealed domain context.
3. Verify real local PostgreSQL and PostgREST: consent/grant/current actor, expiry/revocation, no-write preparation, exact save/replay, disconnect rollback/retry. Keep both migrations dormant until verified. No paid service is introduced.
4. Integrate upstream in the existing isolated worktree, run scoped regressions/typecheck and production build, apply approved migrations and deploy. Independently verify live deployment and unchanged public exposure.
5. Seal only the reviewed catalog effect graph; provision a new expiring MAVERICK/operator/client binding. Run the real Codex connection test, prepare a clearly fictional catalog record, and present the exact OPS approval to Jackson. Never substitute service SQL or automated approval for his decision.
6. Update Bible and evidence with exact local/live/host proof and remaining operator action. Preserve shared work and existing financial trial state.
