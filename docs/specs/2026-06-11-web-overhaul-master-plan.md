# OPS-Web Overhaul — Master Plan

**Status:** Active — P1 (decisions) complete · P2 (shell) shipped · P3.1 (Books) shipped
**Decision authority:** Jackson, 2026-06-11
**Document role:** Program-level spec for the full production-polish overhaul of OPS-Web. Every phase session works from this document. Each phase runs its own spec → plan → build → verify cycle; this document defines the destination, the verdicts, and the rules. It does not duplicate per-surface design detail — that is produced inside each phase.

---

## 1. Mission

OPS-Web must look and feel like a product people confidently pay for: uniform, consistent, polished, production-ready on every surface. The bar is the design system's own reference points — xAI, SpaceX mission control, Apple Pro apps. Anything that reads as a loose interpretation, a tech demo, or an old aesthetic fails.

**Definition of done:** every surface in the final IA conforms to `ops-design-system/project/DESIGN.md`, passes the P4 cross-cutting checklist, and survives the P5 full walkthrough with a punch list at zero.

---

## 2. Final information architecture — 8-item nav

The sidebar goes from 16 items to 8. No dividers-as-apology: the list is short enough to need no grouping debate.

| # | Label | Route | Absorbs | Redirects (must preserve query params) |
|---|-------|-------|---------|----------------------------------------|
| 1 | DASHBOARD | `/dashboard` | — | — |
| 2 | PROJECTS | `/projects` | Map (becomes third view mode: canvas / table / map) | `/map` → `/projects?view=map` |
| 3 | SCHEDULE | `/schedule` | — (rename of Calendar) | `/calendar` → `/schedule` |
| 4 | PIPELINE | `/pipeline` | — | — |
| 5 | BOOKS | `/books` | Estimates, Invoices, Accounting (A/R), Expenses (currently orphaned components, no route), Cashflow placeholder | `/estimates` → `/books?segment=estimates` · `/invoices` → `/books?segment=invoices` · `/accounting` → `/books?segment=invoices&view=aging` (exact mapping decided in-phase) · `/money/cashflow` → `/books` |
| 6 | CATALOG | `/catalog` | Products, Inventory (segments: PRODUCTS / STOCK, matching iOS Catalog tab) | `/products` → `/catalog?segment=products` · `/inventory` → `/catalog?segment=stock` · `/products/[id]/options` → equivalent catalog path |
| 7 | CLIENTS | `/clients` | — (rebuilt from scratch) | — |
| 8 | SETTINGS | `/settings` | Team (members, invites, roles, permissions become a Settings section) | `/team` → `/settings?section=team` (exact param decided in-phase) |

**Removed from the default nav** (invisible to all non-flagged companies):

- **Inbox** — shelved (see §3). Route stays flag-gated for Canpro.
- **Calibration** — Phase C surface; flag-gated, reachable for Canpro only.
- **Agent Queue** — Phase C surface; appears in the nav (bottom group, near Settings) **only** for companies with the Phase C flag. Everyone else never sees it.
- **Testing Grounds** — dev-only; must not ship in the production build/nav.
- **Cash Forecast placeholder** (`/money/cashflow`) — absorbed by Books.

**Redirect rule:** every retired route redirects to its absorbing surface. Notification `action_url`s stored in the database (e.g. `/invoices`, `/calendar`) must keep resolving. P4 verifies this end-to-end.

**Schedule rename scope:** route, sidebar label, top-bar title, en + es dictionaries, FAB actions, and all internal links say "Schedule." The current state — sidebar "Calendar," top bar "Schedule" — is exactly the class of drift this overhaul exists to kill.

---

## 3. Inbox / Phase C posture (decided 2026-06-11)

- **The in-app Inbox UI is shelved.** It does not appear in the nav and gets no polish work until the initiative is explicitly resumed. Do not route new features through an in-app inbox surface.
- **The email engine continues headless.** It pipes data (drafts, extracted leads, learning) into and out of the user's connected Gmail / Outlook mailbox. The user's real inbox is the inbox.
- **Phase C (agent functionality) is Canpro-only** until it is 100% production ready. The app must work fully without Phase C; Phase C is a supercharger layered on top, never a dependency.
- **Calibration and Agent Queue** are Phase C operator surfaces: gated behind the existing per-company feature flag, visible/reachable only for flagged companies (today: Canpro). The P2 session must read the current flag mechanics (per-company `inbox_ui` flag, middleware redirects, `feature-flag-definitions.ts`) and extend them to cover nav visibility — verify in code, do not assume this document's naming is exact.

---

## 4. Surface verdicts

| Surface | Verdict | Notes |
|---------|---------|-------|
| Shell (sidebar, top bar, avatar menu, drawers, FAB) | **Rebuild from scratch** | Phase 2. See §5 |
| Books | **Build new** | Replaces three old single-file pages + orphaned expense components |
| Catalog | **Build new** | Inventory page is a 116-line stub; Products is mid-size and absorbed |
| Clients | **Rebuild from scratch** | Adopt the floating-window interaction model (see below) |
| Dashboard | Keep — light polish pass | User-approved baseline |
| Settings | Keep — renovate + absorb Team | Gains team/members/roles section |
| Pipeline | Keep — conformance audit only | Recent heavy build |
| Projects | Keep — absorb Map as view mode | Recent heavy build |
| Schedule (Calendar) | Keep — rename + conformance audit | Recent heavy build |
| Estimates / Invoices / Accounting / Team / Map / Cash-forecast placeholder | **Absorb & retire** | Old pages deleted once parity confirmed |
| Inbox / Calibration / Agent Queue | **Gate, don't touch** | §3 posture |
| Testing Grounds | **Remove from production** | Dev tooling only |

**Feature-parity rule (non-negotiable):** any session absorbing or rebuilding a surface must read the old surface's code top-to-bottom, produce a capability inventory, and reach 100% parity in the replacement — or list descoped items explicitly and get Jackson's sign-off. No silent feature loss. "The old page had it" is a launch blocker, not a footnote.

**Interaction model:** the floating workspace window is canonical for entity detail (already true for Projects — `/projects/[id]` survives only as a thin SEO/deep-link fallback). The Clients rebuild adopts the same model: click a client → client workspace window; `/clients/[id]` becomes a thin fallback. Any control anywhere in the app that still *navigates* to a dedicated detail page instead of opening the window is a bug (P4 sweeps for these).

---

## 5. Phase 2 — shell rebuild scope

Everything else lives inside the shell, so it goes first. One spawned session: **`WEB OVERHAUL - P2-1`**.

In scope, as one cohesive design (mockups before code):

- **Sidebar** — 8-item nav per §2; revisit the hover-expand rail behavior (currently collapsed-at-rest 72px → 256px overlay on hover) as an explicit design decision; mobile drawer; company header; user section.
- **Avatar menu** — full redesign. The current generic dropdown (Settings / OPS Website / Courses / Download iOS App / Sign Out) is off-brand; note the "Download iOS App" item currently links to `"#"` — dead link, fix or cut.
- **Top bar** — page titles must come from a single i18n-compliant route registry shared with the sidebar (today `top-bar.tsx` hardcodes English titles in a local map — this is the Calendar/Schedule drift's root cause); breadcrumbs, search affordance, sync indicator, undo button all re-examined as one composition.
- **Notifications drawer + quick-actions edge tabs** — redesign for cohesion with the new shell; the user has flagged the drawer as clunky.
- **FAB** — audit every action against the new IA (`fab-actions.ts` route handlers point at `/expenses`, `/inventory`, etc. — retarget to absorbing surfaces).
- **Sidebar data hooks** — inbox unread badge is removed with the Inbox nav item; agent-queue badge renders only under the Phase C flag.

Constraints: DESIGN.md §9 components (no accent on nav, 2px text-2 active bar, 6px sidebar radius), z-index scale from `OPS-Web/CLAUDE.md`, work in a git worktree (never branch-switch the primary checkout).

---

## 6. Phase plan

| Phase | Content | Session(s) |
|-------|---------|-----------|
| **P1** | Decision record — this document | done (2026-06-11) |
| **P2** | Shell rebuild (§5) | `WEB OVERHAUL - P2-1` |
| **P3** | Surface waves, priority order: **3.1 Books** (kills 4 nav items) → **3.2 Catalog** → **3.3 Clients** → **3.4 Settings + Team absorption** → **3.5 Projects map-view absorption** → **3.6 Schedule rename** → **3.7 Dashboard + Pipeline + Schedule conformance/polish passes** | `WEB OVERHAUL - P3-1` … `P3-n` |
| **P4** | Cross-cutting consistency sweep (§7) | `WEB OVERHAUL - P4-1` |
| **P5** | Final walkthrough: run the app, every surface, multiple viewports, screenshot gallery, punch list to zero | `WEB OVERHAUL - P5-1` |

**Sequencing rules:** P2 lands before any P3 wave starts (pages land inside the corrected frame). P4 starts only when all P3 waves have landed. Spawn titles follow the convention exactly; ordinals continue across sessions (next spawn in a phase = highest existing ordinal + 1).

**Branch & publication rule (Jackson, 2026-06-11):** the entire overhaul builds on the **single local feature branch `feat/web-overhaul`**, checked out in the `ops-web-overhaul-p2-shell` worktree (directory name is historical — don't rename it, the branch inside is what matters). No per-task, per-phase, or per-surface branches. **Nothing is pushed to GitHub until Jackson explicitly authorizes publication.** Consequence: overhaul waves land **sequentially** on the branch — one overhaul session works it at a time; confirm no sibling overhaul session is mid-wave before starting. Commits stay atomic per the standard rules; this changes where work lands, not how it's committed.

**Transition rule (no broken nav, ever):** P2 ships the rebuilt shell on a **data-driven nav registry** designed for the end-state 8-item IA, but populated only with routes that exist at ship time — Estimates / Invoices / Accounting / Products / Inventory entries remain (inside the new shell design) until their absorbing surface lands. The Schedule rename may ride P2, since `/calendar` already exists. Each P3 wave's landing commit updates the registry — swapping the absorbed entries for the new surface and adding the §2 redirects in the same commit. At no point does the nav link to a route that 404s, and at no point does a surface lose its nav entry before its replacement is live.

**Per-session protocol (every phase session, mandatory):**

1. Read this document, `ops-design-system/project/SKILL.md`, `README.md`, and `DESIGN.md` (+ `colors_and_type.css` tokens), and relevant `ops-software-bible` sections.
2. Read the old surface's code top-to-bottom for the parity inventory (§4).
3. Run its own brainstorm/spec → plan → build cycle; mockups/wireframes before code for any new layout.
4. Verify in the running app with screenshots before claiming done.
5. Commit atomically (stage by name, no AI attribution), respect parallel-session WIP, update the relevant bible section in the same session.

**Design enforcement (added 2026-06-11 after wave output was judged a "cheap replica" of the system):**

- `ops-design` is **not an invocable skill** — it is a directory of files. Reading it is mandatory but reading alone has demonstrably NOT produced on-brand output. The following are hard requirements, not suggestions.
- **Invoke these registered skills via the Skill tool, by exact name, at the stated moment:** `frontend-design:frontend-design` or `custom-skills:interface-design` before building any page or component; `ops-copywriter:ops-copywriter` before writing any user-facing string; `animation-studio:animation-architect` before any motion/transition work; `custom-skills:widget-builder` for anything widget-shaped. If a skill fails to load, say so in the session log rather than silently proceeding.
- **Composition is traceable or it doesn't ship:** every new panel, table, toolbar, header, and strip must trace to a reference component in `ops-design-system/project/ui_kits/ops-web/` or to DESIGN.md §9 (components) / §10 (widget anatomy). Token-compliant colors on an off-system composition still fails. If no reference exists for what you need, mock it and get Jackson's approval before writing code.
- **Hard done-gate:** before reporting a wave complete, invoke `custom-skills:audit-design-system` against every file created or modified in the wave, fix every high-severity finding, and include the audit summary in the landing report. A wave without its audit attached is not done.

**UX-judgment gate (the overarching principle — Jackson, 2026-06-12):**

Critical thinking about layout, organization, and information presentation **is the work itself**, not a review step. Every presentation decision must be *reasoned from the human's situation* — what they're trying to accomplish right now, how often they do it, what state they're in, what they should see first — never merely *rendered* from the data model, the feature list, or this spec's letter. At the mockup stage, before Jackson sees anything, run an explicit information-architecture critique: for **every element on the screen**, answer *"why is this the right presentation, for this user, at this moment?"* Any element whose only justification is "the option/data/feature exists" gets redesigned, not shipped. Jackson reviews taste; coherence must already be guaranteed — he must never have to catch a design-thinking failure retroactively.

Canonical failure: Books rendered side-by-side QuickBooks AND Sage connection cards because two providers exist in the data model — but a user picks one provider, once, ever. Correct: a single CONNECT ACCOUNTING SOFTWARE entry point → brief provider flow → compact live badge once connected → settings/disconnect/switch in a modal behind the badge. Derived applications (illustrative, NOT exhaustive — the principle is the requirement): either/or choices collapse to one entry point; prominence proportional to frequency of use (once-ever setup never owns permanent prime space); state-aware layouts that show the user's current reality, not all possible realities; the schema is not the interface; verbs out of scan surfaces; progressive disclosure over page acreage; ruthless omission — every element earns its place.

---

## 7. P4 cross-cutting sweep checklist

Run as systematic greps + a full visual walkthrough. Every item is pass/fail per surface:

- [ ] Every project click opens the workspace window — zero navigations to `/projects/[id]` from interactive controls (thin fallback route excepted)
- [ ] Every client click opens the client window (post P3.3)
- [ ] Copy voice (ops-copywriter pass): UPPERCASE authority, `//` panel prefixes, `[brackets]` micro-text, no exclamation points, no emoji, no "Welcome back!"-class copy
- [ ] Numbers: always JetBrains Mono, tabular-lining, slashed zero, formatted (`$12,480`, `87%`), `—` for empty — never `N/A`, never raw floats
- [ ] Empty / loading / error states present and on-spec for every surface
- [ ] i18n: zero hardcoded user-facing strings (top-bar route titles are a known offender); en + es dictionaries complete
- [ ] Icons: `lucide-react` only (no `@carbon/icons-react` imports), sizes 16/20/24/32, `currentColor`, monochrome
- [ ] Accent `#6F94B0` only on primary CTAs + focus rings — never nav, links, toggles, tags, icons
- [ ] Radius / spacing / color trace to tokens (run `custom-skills:audit-design-system`)
- [ ] Motion: single easing `cubic-bezier(0.22, 1, 0.36, 1)`, no spring/bounce, `prefers-reduced-motion` fallbacks everywhere
- [ ] Z-index: touched components migrated to the new scale
- [ ] All §2 redirects work; old notification `action_url`s resolve; no orphaned routes or dead dictionary keys
- [ ] Permissions: granular `has_permission` checks only — zero role-name filtering
- [ ] No box-shadows on dark surfaces; glass tiers used correctly; no triple-stacked glass

---

## 8. Inherited non-negotiables

`DESIGN.md` is law for every visual decision. Root `CLAUDE.md` + `OPS-Web/CLAUDE.md` rules apply in full (perfection standard, no deferral, commit discipline, bible updates). Where this document and the design system conflict, the design system wins and this document gets corrected.

---

## 9. Decision log

| Date | Decision | By |
|------|----------|----|
| 2026-06-11 | 8-item nav approved (Dashboard, Projects, Schedule, Pipeline, Books, Catalog, Clients, Settings) | Jackson |
| 2026-06-11 | Calendar renamed Schedule (route + labels) | Jackson |
| 2026-06-11 | Rebuild from scratch: shell, Books, Catalog, Clients | Jackson |
| 2026-06-11 | Inbox UI shelved; email engine runs headless through the user's connected Gmail/Outlook | Jackson |
| 2026-06-11 | Phase C (agent, Calibration, Agent Queue) Canpro-only until 100% production ready; app never depends on it | Jackson |
| 2026-06-11 | Map absorbed into Projects as view mode; Team absorbed into Settings | Jackson |
| 2026-06-11 | P2 sidebar: hover-to-expand rejected on live review (jarring); fixed 72px instrument rail with right-flyout tooltip labels approved and shipped (cc4f78a6, shell spec §9 revision) | Jackson |
| 2026-06-11 | Single local branch `feat/web-overhaul` for the entire program; no per-task branches; no GitHub pushes until explicitly authorized; waves land sequentially | Jackson |
| 2026-06-11 | **P3.1 Books direction A ("Instrument Strip") approved** — Jackson delegated the pick ("whatever you think is the best UX") across three live-reviewed mockups; A chosen for iOS-pattern fidelity, table width, and clean permission degradation. Mockups + capability inventory + plan: `docs/{design/2026-06-11-books-mockups, specs/2026-06-11-books-capability-inventory.md, plans/2026-06-11-books-surface.md}` | Jackson (delegated) |
| 2026-06-11 | **P3.1 Books shipped** — `/books` with ledger strip (NET/CASH FLOW/A-R/JOBS) + 4 segments (INVOICES · ESTIMATES · EXPENSES · SYNC); zero descopes against the capability inventory. §2 row-5 mapping confirmed: `/accounting` → `/books?segment=invoices&view=aging` (tab-aware: expenses→expenses, integrations→sync, import→sync+import); `/books/cashflow` (iOS forecast_dip action_url) added to the redirect map. Registry gained `anyOfPermissions` (route gate = invoices.view ∨ estimates.view ∨ expenses.approve ∨ accounting.view; `.approve` because the web expenses segment is the review hub); `/books` joined the `accounting` feature-flag route list. Old pages deleted after live verification (gallery in `docs/design/2026-06-11-books-mockups/verification/`). Known pre-existing red: `tests/unit/inbox/ai-draft-provenance.test.ts` (lifecycle-learning flag drift, unrelated to this wave) | Session P3.1 |
| 2026-06-11 | Market-intel research (`WEB OVERHAUL - P3-1-1`) folded into Books: A/R-at-a-glance + per-job profit are the trades-loved surfaces; nothing auto-sends in the operator's voice; plain-language labels over accountant-speak | Session P3.1 |
| 2026-06-11 | **P3.1 adversarial review remediated** — 28/33 audit findings confirmed and fixed in-wave (local-date period boundaries, date-based `status=overdue` A/R drill, hydration-safe segment default, full 11px/token/motion/i18n conformance, widget `/new` dead links). Two items pending Jackson sign-off in inventory §8: MetricsHeader mini-viz presentation drop (data parity is complete) and the pre-existing `accounting.view`-only empty-data persona | Session P3.1 |
| 2026-06-12 | **P3.2 Catalog direction D ("Workbench") approved + shipped** — Jackson rejected pure-efficiency framing ("think about the flow, what they're likely looking at, how they interact") on the first A/B/C mockups; a 5-agent research workflow (trades-owner forums + reviews, inventory-UX patterns, iOS parity, data feasibility) + synthesis produced **direction D**, organized around three ranked flows — buy-run triage, count/receive, price-book upkeep — then hardened by a 3-lens adversarial critique (owner realism / design-system / overpromise-vs-live-data). Jackson approved D for the build. `/catalog` ships with PRODUCTS / STOCK segments (mirrors iOS Catalog tab): a 3-tile **supply strip** (STOCK HEALTH / ON-HAND / PRODUCTS) where every tile is an owner question with a working pivot and honest zero states (uncosted tenants see `—`, never fake zeroes; UNTRACKED is its own health bucket — threshold-less stock is never "OK"); STOCK = flat variant-aware rows (family + option-value label) with inline QTY editing (set or signed delta, **audited** to `inventory_deductions` keyed by `catalog_variant_id`, advances down the column), buy-run pivot with COPY LIST/PRINT + coverage-honest BUY-TO-THRESHOLD, opt-in GROUP::FAMILY, a right detail drawer (quick-adjust + unit-cost + 3-level threshold-cascade sources + used-in reverse links + web/iOS adjustment ledger), bulk delete, and a kebab (MANAGE categories/tags/units/threshold-defaults · saved counts · CSV import); PRODUCTS = tri-coupled COST/PRICE/MARGIN inline cells + NO-COST worklist + CONFIG counts + quick-add. New full editor at `/catalog/products/[id]` (base fields + options + modifiers + recipe — the estimate-builder read path survives intact) **fixes the iOS "VIEW ON WEB →" deep link to `/products/{id}` that 404s today** (`ProductDetailView.swift:1054`). Data layer reads `catalog_*` directly (RLS `company_isolation` on `{public}` verified) — the variant-blind `inventory_*` compat views are abandoned, not extended; snapshots write `catalog_snapshots` directly. Registry swap: Products+Inventory → CATALOG (order 9, `Library` icon, anyOf `products.view`/`inventory.view`); middleware 308s `/products`→`?segment=products`, `/inventory`→`?segment=stock` (action=new preserved), `/products/{id}`+`/products/{id}/options`→`/catalog/products/{id}` — **all verified live**. Dead `products`/`inventory` feature-flag config removed (never real `feature_flags` rows; only pipeline+accounting exist). Capability inventory + §6 scope-additions: `docs/specs/2026-06-11-catalog-capability-inventory.md`; mockups + rationale: `docs/design/2026-06-11-catalog-mockups/`. Live-verified on port 3017 with real Maverick data (67 SKUs / 20 products / 3 costed → $500 on-hand / 7 missing cost / 45% avg margin); both segments + all 3 redirects confirmed (route 200, tsc 0 catalog errors, eslint clean). Note: shared preview browser contended with an active sibling Books session (HMR churn) — stock + products segments captured between collisions. Commits `f47cb2ba`…(retirement) | Jackson (D approved) / Session P3.2 |
| 2026-06-12 | **P3-3 design-conformance remediation landed** — the 2026-06-11 audit punch list (20 high / 42 med / 14 low) driven to zero across shell + Books, except the control-size findings VOIDED by Jackson's 2026-06-11 sizing ruling. Systemic: slashed-zero + tabular numerals enabled globally (`"tnum" 1, "zero" 1` on every mono site); DESIGN.md §9 amended with the ratified **compact workbar tier** (28px controls / 22–24px chips for data-dense workbars; 36px = standard button not a floor; §15 44px row marked iOS-only) — NOTE: `ops-design-system/` is not a git repo, so the amendment is a file edit with no commit. Shell: accent stripped from edge-rail chrome (EdgeTab "accent" member deleted; attn states glaze tan), top-bar blur-12 chip glass → flat interactive surfaces, sidebar → glass-surface tier + top gradient, all 0.04/0.06 hairlines → --line/--glass-border, 22 sub-11px sites → 11px floor, Cake Mono confined to 14/11/22 roles, breadcrumbs → kit mono-11 `//` pattern with Cake leaf titles, drawer headers → kit `// TITLE` widget anatomy, drawer paddings centralized (EDGE_DRAWER_PADDING), durations → 150/200ms tokens + keyframes on the OPS curve + full reduced-motion coverage. Books: per-row icon toolbars → one labelled ACTIONS overflow (zero capability loss), coach-mark empty states → tactical `0 INVOICES` form, es/notifications + es/quick-actions fully translated ("__" TODO markers deleted), payment terms + sync statuses localized, `Intl.NumberFormat("en-US")` ×3 → active locale, shared Button → Cake Mono 300 14px. Deviations from audit fix-text, by judgment: tooltip chips at 4px radius (12px on a 26px chip = pill, §14 ban), ledger strip keeps 16px internal gap as one composite instrument (documented exception), rail badges normalized to surface-active fill. Done-gate: `custom-skills:audit-design-system` re-run over all 27 touched files — **zero high findings**; 3 gate residuals fixed in `8a154b74`; pre-existing out-of-scope items flagged for P4 (`.ops-badge` rounded-sm legacy utility, unused `.ops-card-accent` anti-pattern utility, Calibration/pipeline legacy easings, table-v2's own Cake-12 workbar labels now non-compliant with the amended §9). `npx tsc --noEmit` clean; before/after verified live on port 3017 (slashed zeros, flat top-bar chips at 0.04/0.10/5px, semantic-only rail stripes confirmed via computed styles). Commits `8aee46af`…`8a154b74` (9 atomic, by theme) | Session P3-3 |
| 2026-06-12 | **P3-5 Books table + toolbar refinement landed** — the three punch-list items (`docs/audits/2026-06-12-books-refinement-punchlist.md` §2/§3/§4) to done. **§2 architecture decision (delegated "decide and document"):** the projects/pipeline table-v2 are full TanStack data grids (saved views, virtualization, inline cell-edit, zoom/density, domain-typed column registries, ~26 files each); copy-adapting a third time imports machinery read-mostly registers never exercise, and extracting the framework wholesale would destabilize two shipped surfaces. Chose a **scoped presentational extraction**: new shared `src/components/ui/register-table/` (`RegisterTable` + cell atoms `TableNumber`/`TablePrimary`/`TableMeta`/`TableMono`) reproducing the table-v2 row anatomy (mono-13 number, Mohave-14 name, mono metadata, earth-tone `Tag` status, one labelled ACTIONS overflow) without the grid framework; Books invoices + estimates adopt it, replacing the two hand-rolled `<table>`s. Projects/Pipeline untouched (zero risk); converging them onto the shared atoms is **deferred debt**. **Naming:** a pre-existing **unused** `ui/data-table.tsx` (generic admin grid: column-visibility toolbar, pagination, no row-click/actions — wrong fit) collided with the intended name and is dead code (zero consumers) → new module named `register-table` to avoid touching a file this wave didn't create; the dead `data-table.tsx` is flagged for separate cleanup. **Scope corrections (punchlist imprecision, verified in code):** the expenses segment mounts `ExpenseReviewDashboard` (a master-detail review queue, no `<table>`) and the A/R aging view has no `<table>` (aging bars + top-clients list + breakdown cards) — neither converted; converting either would be a regression. **§3** — list/aging promoted from a filter-row `FilterChips` pair to a shared `SegmentControl` in the stable `workbar`, pinned far-right (Jackson-approved placement), present + identically positioned in both states (verified live: toggle container right edge/top **identical** list↔aging at 1290/286); search is list-only and sits left of the toggle so it never shifts; state-aware (rendered only when the user can reach both views); `ArAgingView.onBackToList` divergent control removed (workbar toggle is the single way back). **§4** — inline New Invoice / New Estimate buttons removed from workbars + empty states (FAB owns creation, `fab-actions.ts`, same perms); empty states state the fact only (`0 INVOICES` / `0 ESTIMATES`), no coach-mark/button per DESIGN.md §2; also drops the last accent from these data-dense workbars. **Process:** mockup approved pre-code (toolbar + row); UX-judgment gate applied to the whole toolbar (every element justified). **Done-gate:** `custom-skills:audit-design-system` over all touched files — **zero high findings** (no bare hex; no raw rgba — row hairline → `border-border-subtle` token; arbitrary-px + `rounded-[4px]` match the shipped kit/compact-tier conventions and the shared `Tag`/`FilterChips`; the systemic `rounded-[Npx]`→named-token question is pre-existing codebase-wide, noted for P4). **No dictionary strings changed** (all keys reused: `view.list`/`view.aging`/`segment.*`/`*.empty.*`/`*.table.*`) → ops-copywriter n/a. `npx tsc --noEmit` + `eslint` clean; verified live on 3017 (invoices list full-width, aging view, estimates), no console errors. Commits `fc15c306` (primitive) + `f54ca6da` (adoption + toolbar) | Session P3-5 |
| 2026-06-13 | **P3.3 Clients rebuilt (Direction B) + retired** — Jackson picked **Direction B** (lean list + tabbed window) from two inline-rendered mockups, then the table-foundation decision (recon surfaced that "use table-v2" conflicts with B's lean design): **`RegisterTable`** — the P3-5 presentational extraction of table-v2 — so "adapt the table-v2 definition" is satisfied without forking the heavy grid or adding DB work. Both descopes approved: **D1** one canonical table (no card/table toggle; sub-contact peek moves into the window), **D2** create folds into the window's `creating` mode. **List** (`/clients`): a slim rose **A/R banner** ("N clients owe $X — oldest Nd", chase→OWES filter) + search + one accent `+ NEW CLIENT` + ALL/WITH PROJECTS/OWES/NEW chips + a glass `RegisterTable` (client/contact/projects/outstanding/last-seen); the scope-aware `clients.view` gate is preserved verbatim; per-client outstanding + recency are real client-side aggregates over the shared invoice/project caches — **no new DB** (join on `invoices.client_id`, NOT the 100%-NULL `client_ref`; exclude paid/void/draft/written_off; "oldest" = oldest **overdue**). **Window**: a `ClientWorkspaceContainer` reusing the generic `ProjectWorkspaceWindow` shell directly + a cloned `ClientViewingTabs` (own layoutId); viewing/editing/creating modes; tabbed dossier **CONTACT** (contact actions + inline sub-contact CRUD + notes) / **PROJECTS** (active+done, row→project window) / **MONEY** (invoiced/paid/outstanding/overdue + invoice list + paid-bar, gated `invoices.view`) / **ACTIVITY** (real composed timeline: projects + invoices sent/paid/past-due + won opps). New cache-shared hooks `useClientOutstandingMap`/`useClientFinancials`/`useClientActivity` (no query-key churn). **Wiring**: `client-workspace` window type + `openClientWindow` + `?openClient=` deep-link (`=new`→creating) in window-store + dashboard-layout; FAB + widget entity-open retargeted; `/clients/[id]` + `/clients/new` → param-preserving redirects (command-palette/pipeline/inbox route-opens land on the window via the redirect, mirroring projects). **Retired**: client-detail popover + store, legacy create/edit client modals, the `create-client` window type (all callers retargeted). **R8 reconsidered (design judgment):** client CRUD stays toast-only — self-action rail notifications are noise. Full UI/UX skill stack invoked (animation-architect/frontend-design/interface-design/ops-copywriter/wireframe/ui-ux-pro-max/data-visualization) + `audit-design-system` done-gate (accent on CTA + focus-rings only; no bare hex; 11px floor; mono tabular) + a **4-lens adversarial review workflow** (correctness/permissions/parity/design) — no critical/high bugs; review-driven hardening landed (clients.edit/create gates on the form + EDIT button, not-found state, restored create funnel telemetry, A/R "oldest-overdue" + past-due activity correctness, money-tile scale). P4 flags: shared `RegisterTable` keyboard focus-ring (Books-owned → spawned `WEB OVERHAUL - P3-3-1`); command-palette/pipeline/inbox direct-open retarget (works via redirect today). `tsc` 0 / eslint clean; live-verified on 3017 with real Maverick data (67 clients / $38,174.88 A/R) — list, row→window, all 4 tabs, edit, create-via-redirect, [id]+new redirects, deep-link (shared preview contended with the sibling Books session + a slow gmail-review-items API; verified between collisions + server-side 200s). Spec `docs/specs/2026-06-13-clients-rebuild-implementation.md`; mockups `docs/design/2026-06-12-clients-mockups/`; inventory `docs/specs/2026-06-12-clients-capability-inventory.md`. Commits `813a24d8`…HEAD (9 atomic, by theme) | Jackson (B + descopes) / Session P3.3 |
| 2026-06-13 | **P4-1 cross-surface visual cohesion audit landed (report only, no source changes)** — live 12-shot visual pass on 3017 + 3 read-only code agents + first-hand primitive source reads, against DESIGN.md/kit. Verdict: shell + Books + Clients read as one product (strong baseline); **Catalog is the lone outlier — it re-derived the kit** (forked `CatalogSegmentControl` at off-scale Cake-13, hand-rolled `<table>`s + per-row icon actions, forked Tag/FilterChips/DrillChip, Button-override CTAs, raw `<select>`/rgba) = the cheap-replica failure localized to one surface. **Token/primitive pass WARRANTED (yes):** (1) extract one `InstrumentStrip` primitive — Books `// LEDGER` vs Catalog `// SUPPLY` are twins with 8 drifts incl. an off-curve rAF count-up in Catalog; (2) **retire `EmptyState`** — it's itself the banned coach-mark (icon+title+desc+CTA, DESIGN.md §2) so registers correctly avoid it but hand-roll inconsistently → mint a tactical register-empty; (3) unify 3 form-error color dialects (Input #B58289 hardcode / Textarea ops-error / others rose token); (4) delete dead `.glass-subtle` tier; (5) codify A/R-aging current color — Books=olive (on-palette), Dashboard widget=`#417394` off-palette (no blue financial token; accent does NOT leak — verified). Cross-surface decision needed (Jackson): create affordance diverges (Books FAB-only vs Clients `+ NEW CLIENT` vs Catalog `+ ADD`). Class-B (Dashboard/Pipeline/Schedule/Projects) catalogued as upcoming-wave scope, not defects (universal `tracking-wider`→`tracking-[0.16em]`; Schedule's 3 legacy easings; RegisterTable convergence is documented deferred debt). Report + gallery: `docs/audits/2026-06-13-cross-surface-visual-cohesion.md` (+ `2026-06-13-visual-cohesion/gallery/`, 12 shots). Folded known residuals (data-table.tsx confirmed removed `55795c06`; .ops-badge/.ops-card-accent; table-v2 Cake-12; focus-ring `P3-3-1`; systemic `rounded-[Npx]`). | Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.1/B1 — `InstrumentStrip` primitive extracted; Books + Catalog strips converged** — the two hand-rolled instrument strips collapsed onto one primitive at `src/components/ui/instrument-strip/` (`InstrumentStrip` section+header / `GlanceGrid` / `GlanceTile` / `TileHero` (optional `tone`) / `TileSub` / `ScopeBadge` / `GlanceTileSkeleton` + the single shared `useCountUp`). Promoted verbatim from the **canonical** Books `ledger-strip` (approved pixels `direction-a-instrument-strip.html`); Books now consumes it **pixel-identically** (every extracted className byte-for-byte the originals — `GlanceTile`=`TileShell`, `TileHero` default branch=`text-data-lg leading-tight text-text tabular-nums`, `GlanceGrid` emits the same classes order-independent; render maps 1:1; per-surface mini-viz `MarginMeter`/`WeeklySparkline`/`AgingRamp`/`DivergingBars` stay slotted, aging-ramp tokens untouched). Catalog `supply-strip` consumes it too, **deleting all 8 drifts**: hero `text-[22px] font-semibold`→`TileHero` (`text-data-lg` 20px, weight 600 retained from the token); `gap-4`→`gap-2`; the hand-rolled **rAF ease-out-quad** count-up (`1-(1-p)²`, off the single curve) → the shared `useCountUp`; `text-[11px]`→`text-micro` (label/Sub/HeroUnit/Mid/cta); literal `cubic-bezier(0.22,1,0.36,1)`→`var(--ease-smooth)` in HealthBar/Meter; tile padding `pt-16/pb-14`→`pt-14/pb-12`; header `mb-3`→`mb-1.5`. **Count-up correctness (load-bearing fix):** `useCountUp` = framer `animate()` on the one OPS curve, now referencing the **`EASE_SMOOTH` token** from `@/lib/utils/motion` (Jackson chose the token import over Books' inline `[0.22,1,0.36,1]` literal — single source of truth); reduced-motion → instant-final (correct alternative for a number reveal). Skills: `animation-studio:animation-architect` (+ auto `web-animations`) confirmed single-curve + reduced-motion; `custom-skills:interface-design` (extraction-fidelity lens); ops-copywriter n/a (zero label strings changed). **Done-gate `custom-skills:audit-design-system` over all 5 touched files — zero high findings** (no bare hex, no raw rgba, every color/size/font traces to a token; the one CRITICAL — easing literal where `EASE_SMOOTH` exists — fixed). `npx tsc --noEmit` clean for the 5 files (the 2 project tsc errors are a **sibling session's** in-flight deletion of `catalog/segment-toolbar.tsx` breaking its own `products-/stock-segment` imports — not mine, not imported by mine); `eslint` clean. **Live-verify GAP (environmental, not code):** Books pixel-identity established by DOM/class equivalence; visual screenshots on 3017 NOT captured — both dev servers had crashed under heavy parallel load (255s response times, 29s cold compiles), and after restarting `overhaul-shell` + bypass-auth the `/books` renderer intermittently hung at 30s and content didn't paint, while `/catalog` was concurrently **broken by the sibling's `segment-toolbar` deletion**. Live screenshots deferred until the environment settles. Atomic commit `8d060628` (5 files staged by name; sibling WIP untouched; no push). | Jackson (EASE_SMOOTH via token) / Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.5/§1 — A/R aging "current" color cross-surface split resolved** — the same A/R aging concept rendered in two color systems: Books AGING (`ar-aging-view.tsx`) colors **current = olive** (`bg-olive` #9DB582, on the canonical healthy→destructive ramp olive → tan → `fin-receivables` → rose → `fin-overdue`), while the Dashboard receivables widget colored **current = steel-blue**. **Finding correction (live-verified, supersedes §5.5's literal):** that steel-blue was **not** an off-palette `#417394` literal — it was `WT.accent` = `var(--color-ops-accent)`, the **runtime accent**, which `PreferencesApplier` overrides per-user from the curated accent palette. On the demo users the accent default *is* `#417394` (the audit eyedropped the rendered pixel and read that); a static `globals.css` read instead returns the `#6F94B0` default — both reconcile via the runtime override. So this was an **accent leak onto a data bar** (DESIGN.md §3: accent never on data bars), strictly worse than a stray literal — any user's chosen accent would color the bucket. **Fix:** new system-wide financial token `--fin-current` = olive `#9DB582` (= `--fin-profit`/`--olive`, the healthy/not-yet-due semantic), wired like the other `fin-*` tokens — design-system `colors_and_type.css` (no commit; ops-design-system is not a git repo) + app `globals.css` (`--color-financial-current`) + `tailwind.config.ts` (`financial.current` → `bg-financial-current`) + `WT.current`. The Dashboard `receivables-aging-widget` BUCKETS repointed at the **full canonical Books ramp** — not just current: `31-60` (was `WT.cost` rose) → `WT.receivables`, `61-90` (was `WT.errorMuted` brick@70%) → `WT.cost` rose, current → `WT.current` olive; `1-30`/`90+` already matched; `heroColor` fallback `?? WT.accent` → `?? WT.current`. The widget is now **accent-free**. **`#417394` retirement (§5.5c):** an exhaustive classification of all 74 `#417394` occurrences found **zero** financial/aging and **zero** internal status-dot uses — every one is a legitimate portal white-label default (20), a user color-picker palette entry (24, incl. the curated "Steel Blue"), or decorative chrome / anti-pattern docstring (26) — so there was nothing to retire from the financial/status palette (the audit's "drives Sent/active status dots" was the same accent mis-attribution; "Sent" = `WT.accent` too, part of the dashboard's accepted data-viz accent language across 23 widgets, which §6 calls "already cohesive" — out of scope, left untouched). DESIGN.md §3 financial table amended (`--fin-current` row + the one-ramp rule; file edit, no commit). **Skills:** `custom-skills:interface-design` (widget-change lens — "color encodes meaning, not the accent") + `animation-studio:data-visualization` (A/R ramp viz — confirmed the healthy→destructive ramp reads correctly); no motion authored (color-token swap; the existing bar height transition already uses the OPS curve), so `animation-architect` not gated; ops-copywriter n/a. **Done-gate `custom-skills:audit-design-system` over touched files — zero high findings** (all 6 WT tokens resolve to defined `--color-*` vars; the only hex added lives in token-definition sites `:root`/tailwind palette; component code is pure token refs). `npx tsc --noEmit` clean for the 4 files (the 1 project error — `estimates-segment.tsx` missing `FileText` — is pre-existing, neither mine nor the sibling's WIP); `eslint` clean (2 pre-existing unused-var warnings in the widget, untouched). **Live-verify (separate port, Jackson-chosen) — succeeded where the §5.1 shared-server attempt failed:** isolated detached worktree at the fix commit on **3019** with its own `.next` (no collision with the active sibling §5 session's 3017 server), dev-bypass as Pete on real Maverick data ($38,175 A/R / 14 invoices); Dashboard receivables **current = `rgb(157,181,130)` olive** (was the runtime accent), full ramp olive→tan→receivables→rose→brick, **identical** to Books AGING (`current` = same `rgb(157,181,130)`); both screenshots captured. **Parallel-session discipline:** a live sibling was mid-§5 pass on `globals.css`/`tailwind.config.ts` — my `globals.css` line was **hunk-staged** (`git apply --cached`) so their `.glass-subtle` removal stayed unstaged; their WIP never entered my index. Atomic commit `02dbb053` (4 files staged by name; no push). | Jackson (separate-port verify, commit now) / Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.3 / §5.4 / §5.6 / §3 B4 / §3 C1 — shared-primitive + token hygiene batch** — six low-risk fixes converging the form family + register primitives onto tokens. **§5.3** form-error color unified: `Button` destructive (`#B58289` + `rgba(181,130,137,…)` hardcodes → `bg-rose-soft text-rose border-rose-line`, hover deepens border to `rose`), `Input` (`border-[#B58289]`/`text-[#B58289]` → `border-rose-line`/`text-rose`), `Textarea` (`ops-error` dialect → rose tokens) — all three now speak one rose dialect; `Textarea` also aligned to the form family (`rounded-sm`→`rounded-[5px]`, label `font-mono text-text-2 tracking-widest` → `font-mohave text-text-3 tracking-[0.08em]`, = Input's label verbatim). `ops-error` left untouched elsewhere (40+ legit call sites; resolves to `#B58289` so the swap is value-faithful). **§5.6** `SearchInput` focus made declarative — the imperative `onFocus/onBlur` `style.borderColor` mutation deleted; all styling → tokenized classes (`border-line`/`bg-surface-input`/`text-text`) + a `focus:border-[rgba(255,255,255,0.20)]` rule matching the Input primitive's focus-within (same value, now declarative not JS). **§5.6** `Tag` re-exported from the `register-table` barrel (full row anatomy from one import). **§5.4** dead `.glass-subtle` tier removed (`--glass-bg-subtle` + `--glass-subtle`, zero consumers verified) + the dead `.ultrathin-material-dark` shim retired (wrong 0.20 border alpha, zero consumers). **§3 C1** Books invoice/estimate/record-payment modals: raw `<select>` → shared Radix `Select` (trigger `h-9` aligned to the sibling date `Input`s; optional-project select uses a `PROJECT_NONE` sentinel since Radix forbids empty-string items; `SelectContent` `z-[60]` renders above the `z-50` dialog — verified in `dialog.tsx`). **§3 B4** Clients new-client **workbar** CTA: hand-rolled accent-OUTLINE `<button>` (no Button import) → `Button variant="primary"` (filled-at-rest). The Clients **empty-state** CTA was intentionally NOT re-added — a sibling's §5.2 `RegisterEmpty` rewrite (no CTA in empty registers, DESIGN.md §2) supersedes it; committing only the workbar CTA hunk **unblocked** that held sibling change. **Skills:** `custom-skills:interface-design` (before editing primitives); `ops-copywriter` n/a (zero strings changed); animation-architect n/a (no motion — auto-injected `web-animations` is a false-positive). **Done-gate `custom-skills:audit-design-system` over touched files — zero high findings** (zero bare hex introduced; every error state on the rose token; `rounded-[5px]` now spans all four form primitives; two INFO residuals only — `rounded-[5px]` and the `rgba(255,255,255,0.20)` focus border are established form-family conventions / pre-existing Input patterns, both already-catalogued systemic residuals). `npx tsc --noEmit` clean for all my files (the 2 project errors are the **sibling's** in-flight `catalog/segment-toolbar` deletion — not mine, not imported by mine); `eslint` clean for my files (the transient `clients/page.tsx` Search/Users-undefined errors belonged to the sibling's mid-rewrite §5.2 state, not my committed version). **Parallel-session discipline (≥3 sibling P4-1 sessions live):** three of my files were commingled with sibling WIP — `globals.css` (their §5.5 `--color-financial-current`), `register-table/index.ts` (their §5.2 `RegisterEmpty` re-export), `clients/page.tsx` (their §5.2 empty-state rewrite + `Search`/`Users` import removal) — each committed via **`git apply --cached`** (index-only; working tree never touched; race-free) so only my hunks landed and every sibling line stayed unstaged for them. **Flag-only (not migrated, per scope):** legacy `segmented-picker` (schedule + dashboard widgets) and legacy `ui/badge` (team/inventory/ops/site-visit) confirmed to live on **pre-overhaul** surfaces — migration belongs to those surfaces' own waves (audit §5.6). Also flagged out-of-scope (not fixed): the form-error-MESSAGE + helper-text font still diverges (Input `font-mono` vs Textarea `font-mohave`) — fixes 1/2 were scoped to error COLOR + the label only. **Live-verify (resolved on a fresh isolated server):** the shared `overhaul-shell` 3017 renderer was stuck under multi-session load (eval + screenshot 30s-timeouts though the server curled HTTP 307; an in-browser Babel load destabilized the CDP renderer) and I would not restart a server siblings depend on — so verification ran on a **fresh detached worktree at HEAD** (`/tmp/ops-web-p41-formverify`, own `.next`, port **3021**) with a clean renderer, dev-bypass as Pete on real Maverick data. Verified by **computed-style inspection** (the right tool for color/token changes per the screenshot tool's own guidance; raw screenshots came out tiny/black — a font/scale artifact of the temp copy not reaching the cake-mono Typekit, not a code defect): **§3 C1** — New-Invoice modal renders **3 Radix comboboxes** (CLIENT/PROJECT/PAYMENT-TERMS) + 1 untouched native line-item picker; CLIENT trigger `role=combobox`, **36px** (`h-9`, aligned to the date `Input`s), `rounded-[5px]`, placeholder live; **§3 B4** — Clients workbar CTA computes `background: rgb(65,115,148)` (filled accent at rest) + black text = the filled primary (not the prior accent-outline); **§5.3** — the rose tokens resolve live (`--rose` `#B58289` / `--rose-line` `rgba(181,130,137,.30)` / `--rose-soft` `rgba(181,130,137,.12)`, rendering on the A/R banner + overdue amounts), and my error states use these exact classes. §5.4/§5.6/§2-textarea are structural — covered by diff + tsc + eslint + audit. 6 atomic commits, each staged by name or hunk-staged: `e02394c6` (§5.3) · `05277f82` (§5.6 SearchInput) · `f27f44f6` (§3 C1 Books selects) · `ba6e68d7` (§5.6 Tag) · `7543a61a` (§5.4 glass) · `44ca2516` (§3 B4 Clients CTA). No push. | Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.2/B2 — `RegisterEmpty` minted; every register empty converged; `EmptyState` retired → `OnboardingHint`** — the flip the audit flagged: the shared `EmptyState` (`ops/empty-state.tsx`) is **itself** the banned coach-mark (icon + title + description + action Button on a left-border strip — DESIGN.md §2 bans coach-marks for empty registers), so the rebuilt registers correctly avoided it but each hand-rolled a divergent tactical empty (Books mono-micro heading + 32px icon, no count/`//`; Catalog mono-11 + `//` + a Mohave help sentence; Clients the **full banned coach-mark** incl. a `+ NEW CLIENT` CTA). Consolidating onto `EmptyState` would have *reintroduced* the ban — so instead: **(1)** new tactical primitive `src/components/ui/register-table/register-empty.tsx` (`RegisterEmpty`) — states the fact only: a hero count (mono `text-data-lg` tabular, `text-text-2`) + `// NOUN` caption (mono `text-micro` uppercase `tracking-[0.16em]`, `//` in `text-mute`) + optional terse `hint`, **no icon / description / CTA**; mirrors the widget hero-number anatomy (§10); exported from the register-table barrel. Two reasoned refinements to the §5.2 mock: dropped the always-`0` `count` prop for an intrinsic render + a `value?: string` (default `"0"`) so a future money/percent register can pass `"$0"`/`"0%"`/`"—"` exactly as §2 enumerates; `hint` uses `text-text-3` (AA) not the mock's `text-mute` (3.4:1, fails AA for informational text). **(2)** Adopted in **Books invoices + estimates, Catalog products + stock, Clients roster, and the inventory items register** — all six now render byte-identical `<RegisterEmpty noun={…}/>` (filtered → "Matches", empty → the entity noun); decorative 32px empty icons dropped (`Receipt`/`FileText`/`Package` imports pruned where dead). **(3) `EmptyState` deprecation — classification corrected the audit's "17 consumers":** a 29-file fan-out workflow found the shared primitive has **exactly 4 direct importers** (the other grep hits are independent *local* `function EmptyState` defs in admin-spec tabs/refunds/inbox, the independent `WidgetEmptyState` wrapper that every dashboard widget routes through, and a test). 3 of the 4 (`snapshots-tab`, `task-list`, `photo-feed`) legitimately want the coach-mark form for non-register onboarding/widget hints → so the audit's **rename** path is right (not remove+retarget): `EmptyState` → **`OnboardingHint`** (`ops/onboarding-hint.tsx`, old file deleted), doc'd as never-for-registers, 3 keepers repointed (behavior unchanged, inherited raw-rgba border resolved to `border-l-border`). The 1 true register among them (`inventory/items-tab`) was **converted to `RegisterEmpty`** instead; its create stays on the toolbar "New Item" button (no behavior loss). **i18n:** reworked to bare-noun keys (`*.empty.noun` / `*.empty.matches`) in pipeline/catalog/clients (en+es) since the primitive renders the count itself; the shared `stock.empty.title` left intact (also consumed by `catalog-page` access state — would have broken it). **Container decision (Jackson): bare on canvas** — `RegisterEmpty` renders where each hand-roll sat (Books/Clients on canvas; Catalog stays inside its existing glass wrapper), not re-wrapped in glass (that's the deferred A2 Catalog re-architecture). **Skills:** `custom-skills:interface-design` (primitive design — every value token-traced) + `ops-copywriter` (the noun/`matches` strings — terse, fact-only, no coaching); `animation-architect` n/a (static empty state; the auto-injected `web-animations` is a false-positive — no motion, matching `RegisterTable`). **Done-gate `custom-skills:audit-design-system` over authored regions — zero high findings** (`register-empty.tsx` fully token-traced, zero literals; the one inherited CRITICAL — `onboarding-hint.tsx` raw-rgba border — fixed; the remaining rgba/hex in the touched Catalog/inventory/task-list/photo-feed files are **pre-existing**, in row markup/skeletons/task-color defaults, not the empty-state regions — they belong to the deferred Catalog C2 / legacy-inventory scope). `npx tsc --noEmit` clean (exit 0); `eslint` clean (the 2 warnings are pre-existing `<img>`/unused-disable in photo-feed, not my rename). **Parallel-session discipline (heavy — ≥3 sibling P4-1 sessions co-edit this worktree/branch):** `register-table.tsx` (sibling P3-3-1 focus-ring WIP) left untouched. The Clients change initially commingled with a sibling's **B4** (workbar CTA→`Button`); that sibling **hunk-staged only their B4 workbar hunk** (`git apply --cached`, commit `44ca2516`), which **unblocked** my empty-state rewrite — I then committed `clients/page.tsx` + the `empty.noun`/`empty.matches` dict keys **cleanly** (diff verified B4-free immediately before staging). Clients create affordance preserved (workbar `+ NEW CLIENT` + FAB) so the B3 inline-create question is untouched. **Live-verify (isolated separate-port worktree — Jackson-chosen):** the shared `overhaul-shell` 3017 window was hung under multi-session load (eval/screenshot/re-attach all 30s-timeout, zero console logs), so — like §5.5 — I stood up a throwaway detached worktree at HEAD + the copied Clients change, own `.next`, `dev:webpack` on **3023**, dev-bypass on real Maverick data (44 invoices / 20 products / 11 clients owing $38,175). Forced each register empty via a zero-result search: **Books invoices, Catalog products, and Clients roster all render the identical `0` + `// MATCHES` fact** — no icon/description/CTA; Clients' banned coach-mark gone, header create CTA preserved; three screenshots captured. (Catalog sits inside its existing glass wrapper, Books/Clients on canvas per the bare-on-canvas ruling — the `RegisterEmpty` primitive itself is byte-identical; identity additionally guaranteed by construction — all six call sites render the same `<RegisterEmpty noun={…}/>`.) Throwaway server + worktree torn down after. Commits `faeb61e3` (primitive) · `90389123` (adopt + i18n) · `30ed3ca0` (OnboardingHint rename) · `70a3f02e` (Clients) — 4 atomic, staged by name, no push. | Jackson (bare-on-canvas) / Session P4-1 |
| 2026-06-13 | **B3 create-affordance ruling (Jackson) — one inline create button per register, FAB stays the global shortcut.** Each register surface keeps **exactly one** inline create button as its single accent CTA: Catalog's `+ ADD` and Clients' `+ NEW CLIENT` stay; **Books' inline create is RESTORED** to the invoices + estimates workbars (`+ NEW INVOICE` / `+ NEW ESTIMATE`) — this **reverses the P3-5 §4 decision** that deferred Books creation entirely to the FAB. The empty-state form stays tactical `RegisterEmpty` (`0 // INVOICES`, no CTA); only the workbar regains the button. All three surfaces render the identical filled-accent `Button variant="primary"` so the treatment matches. Catalog label kept as **ADD** (Jackson, over the more-specific NEW PRODUCT/NEW ITEM alternative). | Jackson |
| 2026-06-13 | **P4-2 Catalog → shared-primitives convergence (audit §3 A1–A6 + §4) landed.** The cheap-replica outlier fixed: Catalog now **consumes** the kit instead of re-deriving it. **A1/A5** forked `CatalogSegmentControl` (off-scale Cake-13) + duplicated FilterChips/DrillChip deleted; `segment-toolbar.tsx` re-exports the shared `SegmentControl`/`FilterChips`/`DismissChip` (mirrors `books/segment-toolbar`) — the visible Books↔Catalog segment break is gone (both now the same JBM-11 28px component; live-confirmed identical by construction + computed style). **A2/A3/A4** the three hand-rolled `<table>`s → shared **`RegisterTable`**: Products + flat Stock are RegisterTable; bare config/trash icons → one labelled **ACTIONS** overflow (Products = `OPTIONS & PRICING` + `DELETE`, exact parity with the retired Sliders+Trash); status forks → shared **`Tag`**; search → `SearchInput`. **Architecture decision (decide+document, mirrors P3-5):** the inline-edit cells (`InlineMoneyCell`/`InlineQtyCell`) are **composed inside** RegisterTable's shell (stop-propagation), NOT baked into the shared atoms — RegisterTable was scoped to *exclude* grid machinery that Catalog's domain cells (parent-controlled advance-down-column, signed-delta audited to `inventory_deductions`, NO-COST nudge, cost/price/margin coupling) already do richer; Projects/Pipeline's table-v2 own the heavy version. RegisterTable extended only with **two generic, presentational, reusable** props — `header: ReactNode` (the Stock select-all checkbox column) + `isRowActive` (the drawer-open master-detail tint) — never a fork. Stock **GROUP::FAMILY** clustered view stays bespoke (no grouping in RegisterTable); **Snapshots'** expandable master-detail shell stays bespoke (out of RegisterTable's flat-row scope, like P3-5 left ExpenseReview/Aging) but converges its anatomy + `RegisterEmpty` + tokens. **A6** all accent-outline `Button variant=secondary`+className CTAs → `Button variant="primary"` (filled-at-rest); product-quick-add service/good toggle → `SegmentControl`. **C1/C2** add-stock + product-editor raw `<select>` → shared Radix `Select` (NONE sentinel); raw rgba literals across cells/stock-drawer/product-editor/manage-modal → tokens (`line-hi` active border, `fill-neutral` dashed underline, `surface-hover-subtle`, `border-subtle`); `rounded-[3px]`→`[4px]`; one 13px Mohave → 14px. **B3 create-affordance ruling implemented** (Catalog `+ ADD`/Books `+ NEW INVOICE`·`+ NEW ESTIMATE` workbar CTAs, both filled-primary; see the ruling row above). Completes the §5.2-deferred container reconciliation (Catalog empty/loading now match Books — bare on canvas + RegisterTable's own glass, not an extra wrapper). **i18n:** new `actions.menu`=`Actions` (mirrors shipped Books) + `snapshots.empty.noun`=`Counts` (en+es), `snapshots.empty` coaching sentence removed; ops-copywriter blessed both. **Skills:** `custom-skills:interface-design` (recomposition lens); `ops-copywriter` (2 new strings); `animation-architect` n/a (no motion authored — token transitions pre-exist; auto-injected `web-animations` is the standing false-positive). **Process:** converged Products + Stock rows mocked inline (with inline-edit cells) and **approved by Jackson before any table code** ("Approve — build it"; "Keep ADD") — spec `docs/specs/2026-06-13-catalog-convergence-p4-2.md`, mock record `docs/design/2026-06-13-catalog-convergence-mockups/`. **Done-gate `custom-skills:audit-design-system` over all 14 touched files — zero unsanctioned high findings** (it caught one missed `product-editor` SortableOptionRow `bg-[rgba(...,0.02)]` the replace_all skipped → fixed `ea3274d7`; the two remaining rgba are the sanctioned spec literals — the `0.18` chip-active border = the shared chip's own literal, and the `0.20` form-focus = the §5.6 Input/SearchInput convention; no token exists for either). `npx tsc --noEmit` **0 errors** project-wide; `eslint` clean. **Live-verify (own isolated `dev:webpack` server, port 3017, fresh `.next`, dev-bypass as Pete on real Maverick data — 20 products / 67 SKUs / $38,175 A/R):** every preserved affordance exercised, **0 console errors** throughout — Products: RegisterTable renders, inline COST edit opens a focused decimal input with **no row navigation** (stop-prop), ACTIONS overflow = `OPTIONS & PRICING`+`DELETE`, olive/dim TAX tags, filled `+ ADD`; Stock: inline QTY (numeric) opens with **no drawer** (stop-prop), checkbox → `1 SELECTED` bulk bar with **no drawer change** (stop-prop), row click → drawer + the clicked row carries `bg-surface-active` (isRowActive), buy-run drill renders BELOW THRESHOLD chip + COPY LIST + PRINT + BUY TO THRESHOLD + CRITICAL-FIRST (empty register = correct, Maverick stock all UNTRACKED), GROUP::FAMILY toggle present; Snapshots: `// SAVED COUNTS` + filled `TAKE COUNT` + `RegisterEmpty` `// COUNTS`; Books: `+ NEW INVOICE` restored, computed `background rgb(65,115,148)`+black = filled primary, segment control identical to Catalog. Screenshots: products / stock / stock+drawer-active / books-restored-CTA. 8 atomic commits `402285ba`…`ea3274d7` (staged by name, no push, no AI attribution). | Jackson (mock approved: build + keep ADD) / Session P4-2 |
| 2026-06-14 | **P3-7 Pipeline conformance/polish pass landed (a KEEP surface — visual/token conformance + the two §6 cohesion-audit divergences; the kanban/stage paradigm is unchanged).** Drove the cross-surface-cohesion audit's §6 Pipeline punch list to zero, then a first-hand file-level sweep of all ~50 pipeline files. **Structural #1 (Jackson-approved mock): per-card bare icon toolbar retired.** Every focused + mobile card carried a bare 4-icon cluster (call/text/details/note) on its face — the exact "icons are metadata, not actions" pattern Books rows dropped. Jackson picked **"Labelled ACTIONS overflow"** over the keep-call+text hybrid; `pipeline-card-actions.tsx` rewritten so the card face is scan-clean — stage-advance affordances stay (moving a deal is the kanban's primary verb), all verbs fold behind one labelled `ACTIONS ▾` (trigger = Books' exact treatment: JBM-11 uppercase `tracking-[0.12em]` `border-border` text-3; reuses the shipped `actions.menu` string). Menu = Log call/Log text/Add note/Details · Schedule follow-up/Assign · Mark Won/Mark Lost/Discard · (Convert) · Archive; inline note input + canManage gating + the won-card Convert entry preserved. Kept the **portaled** popover (not the Radix primitive) — cards live in a drag/scroll context and the menu opens above its anchor near the board edge; the portal/trigger/items are token-clean (glass-dense, border-line, fill-neutral-dim) so the audit gate passes (hand-rolled-where-primitive-exists is a justified WARNING, not a HIGH, given the drag context). **Structural #2 (proceeded as conformance — additive, not a fork): metrics `//` grammar.** Pipeline renders the shared legacy `MetricsHeader` (also map/projects/schedule; Books retired it). Rather than fork or pre-empt those waves, added an **opt-in `slashLabels` prop** to `MetricColumn`+`MetricsHeader` (default off → every other consumer byte-identical); Pipeline opts in, so labels now read `// PIPELINE VALUE` (mono, `tracking-[0.16em]`, `//` in text-mute) matching Books' tile-label grammar. The `InstrumentStrip` GlanceTile (132px) was correctly judged **not to fit** Pipeline's compact floating HUD — the `// LABEL` grammar is the right conformance, not the tall tile-deck. **Token sweep (behavior-preserving, both surfaces):** `tracking-wider/widest`→`tracking-[0.16em]` (~28 sites); off-scale Cake→on-scale (card/panel titles caption-sm 12→14; spine/terminal/drop-zone labels→11; mode-switcher + table toggles Cake-10→**JBM-11 segment voice**; table buttons 12→14; table dialog titles 18→**22**, matching the Books canonical over Catalog's shipped-but-off-scale 18); sub-11px workbar/toolbar labels→11px floor; raw rgba/hex→tokens (border-line/-hi/-subtle/-strong, surface-input/-hover/-active, fill-neutral/-dim, glass-border-active/-strong; health-bar zone colors→financial tokens; mobile swipe strips→olive/rose soft+line; #6F94B0 **accent leaks** on mobile-card tel/mailto/'view details' links→text-2; #93321A/#C4A868→brick/tan; #8A8A8A stage fallbacks→`var(--text-3)`; the drag-overlay's off-token steel `#8F9AA3` Archive affordance→neutral white-alpha/text tokens, brick for Discard); `rounded-full` health bars→`rounded-bar`, `rounded-[3px]` checkboxes→`rounded-chip`; the compact-card box-shadow (unreachable dead branch) removed; `accent-ops-accent` radio leak→neutral; **Add-Lead workbar CTA + table Save CTA → canonical filled-primary** (the screen's single accent CTA each). **Dead code removed:** `pipeline-metrics-bar.tsx` (superseded by MetricsHeader) + `quick-add-form.tsx` — both zero-importer, ~45 carried violations gone. **The surface's only legacy easing** (`globals.css` `pipeline-stale-pulse` `ease-in-out` + brick rgba/hex) → `var(--ease-smooth)` + `rgb(var(--status-error-rgb)/a)` + `var(--brick)`. **Deferred by design (NOT done, documented):** table-mode → RegisterTable convergence — the flag-gated grid is `useVirtualizer` + frozen rails + roving-tabindex inline-edit + saved-views + grouped-stream, none of which RegisterTable supports; converting would regress that behavior (register-table.tsx:11-16 already logs this as deferred debt). Token conformance of the table chrome WAS done in place; the grid structure was untouched. **Skills:** `custom-skills:interface-design` (the two structural judgment calls — card-action treatment + the "does the InstrumentStrip fit" decision); `frontend-design` redirected to interface-design for this dashboard surface per its own scope note; `ops-copywriter` n/a (zero new strings — `actions.menu` already shipped); `animation-architect` n/a (no motion authored — the stale-pulse is a curve/token swap on an existing keyframe; the auto-injected `web-animations`/`data-visualization`/`realistic-3d` hints are the standing false-positives). **Done-gate `custom-skills:audit-design-system` over all ~38 touched files — zero high findings** (find-based scan: 0 bare hex / 0 raw rgba / 0 off-scale Cake / 0 sub-11px / 0 tracking-wider / 0 box-shadow-on-dark / 0 legacy-easing in touched regions; the only residuals are sanctioned — data-driven `rgba(${stageRgb})` stage colors, the §5.6 `rgba(255,255,255,0.20)` form-focus convention, the tokenized `via-background/70` scroll-fade, and `MetricColumn`'s pre-existing `#6B6B6B`/`#EDEDED` trend/value defaults left untouched in the non-slash branch so other surfaces are unaffected). `npx tsc --noEmit` **0 errors in pipeline+metrics**; `eslint` **0 errors** (4 pre-existing warnings — unused page.tsx imports + a table-shell useEffect dep — not mine); **vitest 16/16** (`pipeline-card-actions` 3/3 + `pipeline-focused-card` 13/13; both tests updated to the ACTIONS-overflow flow: trigger name `More`→`Actions`, "Open detail" now a menuitem). **Live-verify (isolated throwaway server — shared 3017 not even running; never contended):** detached worktree at the commit tip + symlinked node_modules + copied dev-bypass `.env.local`, `dev:webpack` on **3033**, headless Playwright (the shared MCP browser was held by a sibling, so an isolated `chromium.launch()` node script drove it — full isolation). The heavy `/pipeline` route cold-compiles in ~275s under load (hence a pre-warm pass); once warm, computed-style probes **confirm**: metric label `// Pipeline Value` letter-spacing `1.76px` (=0.16em) JBM, `//` color `rgb(106,106,106)`=text-mute; **3** `ACTIONS` triggers (JBM 11px, `tracking 1.32px`, `border rgba(255,255,255,0.1)`, text-3) — **0** bare `Log call` icons on any card face; ACTIONS menu items exactly `[Log call, Log text, Add note, Details, Schedule follow-up, Assign to, Mark Won, Mark Lost, Discard, Archive]`; card title 14px cake-mono; Add-Lead CTA `background rgb(65,115,148)`(runtime accent)+black=filled primary. Screenshots: pipeline-after / pipeline-actions-open / books-flip — **Pipeline↔Books now read as one system** (Books `// LEDGER`+`// NET`/`// A-R` tiles + per-row `ACTIONS` + filled `+ NEW INVOICE` ↔ Pipeline `// PIPELINE VALUE`… + per-card `ACTIONS ▾` + filled `+ NEW LEAD`). Console errors = benign env RLS/401 only. Throwaway server + worktree torn down. **Concurrency discipline (3 lanes co-editing this worktree/branch — Settings P3-6 + connect-flow P3-4):** all 38 of my files staged **by name** in 6 atomic commits; verified `git diff` before each stage; the 2 shared metrics files + `globals.css` were mine-only at stage time (full-diff inspected — no sibling WIP present) so staged whole; the 27 dirty `settings/*` + `books/sync/*` + `books.json` sibling files never touched. Commits `ec54b9d8` (dead code) · `493b5e34` (ACTIONS overflow) · `87c6c731` (metrics `//`) · `782dabbb` (focused tokens) · `df1dc7cf` (table tokens) · `d62fe159` (stale-pulse) — staged by name, **no push**, no AI attribution. | Jackson (ACTIONS-overflow mock approved) / Session P3-7 |
| 2026-06-14 | **P3-4 Books accounting-connection UX redesign shipped — the §6 UX-judgment-gate canonical example.** Replaced the two side-by-side QuickBooks+Sage connection cards with the gate's prescribed pattern: ONE entry point (`ConnectPanel`) → glass-dense provider picker (`ConnectAccountingModal`; QuickBooks pre-selected each open, Sage fully selectable — both `/api/integrations/{provider}` routes exist, never QB-hardcoded) → ambient `QUICKBOOKS · LIVE` badge in the workbar chrome (`ConnectionBadge`, olive live / tan offline, reduced-motion live pulse on `EASE_SMOOTH`) → settings/disconnect/switch behind the badge (`ConnectionSettingsModal`). Connected-healthy body = calm `SyncStatusPanel` (`SYS :: SYNCED · time` + a recent-5 reassurance log) — "what would Apple do": invisible when it works, speaks only when it needs you; not a console. New components under `src/components/books/sync/`; `sync-segment.tsx` rebuilt as the state machine (disconnected / picker / connected / offline-reconnect / import). **Built full-CRUD (two-way) per Jackson — read-only is the current testing posture only, never hardcoded:** a real SYNC MODE control (read-only ↔ two-way) + mirror-deletes drive the existing `updateSyncMode(syncDirection, propagateDeletes)`, with an honest "writes paused during testing" note keyed off `writesEnabled` (`ACCOUNTING_WRITE_ENABLED`). Surfaces two dormant data-layer capabilities the old cards never did (auto-sync toggle, sync mode). **Re-justified per the gate (deliberate, surfaced — not silent loss):** the "how sync works" explainer moved to the connect/onboarding flow only (once connected it is noise; prominence ∝ frequency); sync history → the calm recent-5 body log (the full diagnostic scroll dropped; actionable conflicts are the separate future `useAccountingSyncIssues` surface). **No stubs:** the two-way conflict-resolution worklist is mocked + explicitly deferred (sync-issues API is GET-only; pull-only cannot conflict); `triggerSync` "Sync now" stays server-gated. The `view=import` URL contract is preserved (→ `QuickBooksImportTab` via the body's adaptive "Reconcile & import"). **Process (§6):** three-state mockups approved by Jackson pre-code (`docs/design/2026-06-14-books-sync-connection-mockups/`); aesthetic = Apple design intent × SpaceX/xAI; skills invoked — `frontend-design` + `custom-skills:interface-design` + `custom-skills:wireframe` + `ops-copywriter` (en+es `sync.*`) + `custom-skills:audit-design-system`. **Integration fix (`quickbooks-import-tab`, caught by live verify):** now prefers the live QB row (a company can hold sandbox+production rows — the import tab showed NOT CONNECTED beneath a LIVE badge), uses the `AccountingProvider` enum, and routes the pre-existing `#B58289`/`#C4A868` hex error/warning text through `text-rose`/`text-tan`. **Shared primitive:** `SegmentControl` gained an optional `disabled` prop (mode-toggle race fix; additive, all other consumers unchanged). **Adversarial 4-lens review workflow** (correctness/parity/permissions/design): permissions clean; high findings remediated (mode-race `disabled`, import-tab tokens) or held-with-documented-rationale (the explainer + history re-justification, which the brief explicitly told this session to re-judge under the gate); deferred — RecordStat `rgba(255,255,255,0.02)` (pre-existing, no exact token, out of edit region). **Done-gate `audit-design-system` zero-high** (no bare hex / no raw rgba in new files; accent only on CTA+focus; the live badge is olive/tan semantic, never accent; mono tabular numbers; 11px floor). `tsc` 0 / eslint clean. **Live-verified** on an isolated throwaway worktree (`git worktree --detach` + symlinked node_modules + copied dev-bypass `.env.local`, `dev:webpack` port 3041; shared 3017 untouched) as Pete on real Maverick data — all six states screenshotted (`.../verification/`): disconnected · picker · connected (`SYS :: SYNCED`, mode-aware `Sync now`) · settings modal · import (back + workspace, now "Connected") · offline (tan badge + reconnect). 0 console errors from the redesign (the 2 are a pre-existing `create_notification` RPC 401). **Concurrency:** 3 lanes co-editing the worktree (Settings P3-6, Pipeline P3-7) — all files staged by name, diffs inspected, no sibling WIP touched. Commits `373a93e5` (SegmentControl `disabled`) · `4b3ab3d2` (sync redesign + i18n) · `6ddba958` (import-tab fix) — **NOT pushed** per the branch rule, no AI attribution. | Jackson (go) / Session P3-4 |
| 2026-06-15 | **P3-7-1 Named Cake type tokens (systemic follow-up to the P3-7 pipeline pass; Jackson-requested after spotting hardcoded `text-[22px]` titles in the pipeline work).** The Cake display sizes (22/14/11) were expressed as arbitrary `text-[Npx]` app-wide — the `Button` primitive (`text-[14px]`), Books `DialogTitle` (`text-[22px]`), every surface — because the named size tokens bake in non-Cake weights (`heading` 22/**600**, `button-sm` 14/**500**) while Cake is mandatory weight **300**, so all Cake usage deliberately bypassed the named scale to grab just the size. **Fix:** three weight-300, line-height-free size tokens in `tailwind.config.ts` — `cake-display` 22 / `cake-button` 14 / `cake-badge` 11 (the three sanctioned DESIGN.md Cake roles: display=titles/headers, button=buttons/card-titles/labels, badge). Size+weight only, **no line-height**, so `font-cakemono text-cake-*` renders **byte-identical** to the legacy `font-light text-[Npx]` it replaces (font-light kept — now redundant-but-explicit). **Adopted by:** the `Button` primitive (`text-cake-button`, the canonical reference used app-wide), the **Pipeline** surface (all 11 Cake sites → tokens), and Books' modal `DialogTitle`s (`text-cake-display`, the canonical 22px reference). **Deferred (documented, NOT done):** the rest of the app's Cake usages (Catalog/Clients/shell/Settings) still use arbitrary `text-[Npx]` — render is already identical (token value == arbitrary value); a future deliberate cross-surface sweep migrates them. Not done now to avoid forking the type scale across 3 live lanes. **Verify:** `tsc` 0 / `eslint` 0 (4 pre-existing warnings, not mine); token-name parity confirmed (defined ↔ used, exact); **a standalone Tailwind build emits `.text-cake-display{font-size:22px;font-weight:300}` + `.text-cake-button{14px/300}` + `.text-cake-badge{11px/300}`** — authoritative positive proof the classes generate at the right size+weight. (A read-only computed-style probe on the shared 3017 showed 16px/400 — a *false negative*: `dev:webpack` does not hot-reload a `tailwind.config` change and the shared server must not be restarted mid-session with siblings on it; the standalone build is the correct check for a config change.) **Concurrency:** Settings P3-6 committed its WIP concurrently (`9b674fed`); my 3 commits layered cleanly on top, each staged BY NAME (config+button / pipeline dir / the 3 books modals), zero sibling WIP touched, tree clean after. Commits `b65436bc` (tokens + Button primitive) · `539418c5` (pipeline adoption) · `49ec56e2` (books modal titles) — **no push**, no AI attribution. | Jackson (go) / Session P3-7 |
| 2026-06-14 | **P3.4 Settings renovation + Team absorption shipped (the UX-judgment gate was the primary work — a 30-entry flat surface re-architected into a reasoned IA).** **IA (Jackson's ruling, over the left-rail mock):** horizontal domain tabs on the shared kit (icon + label, 2px monochrome active bar — no accent, DESIGN.md §9) over a sub-section `SegmentControl`, Books/Catalog grammar — replacing the bespoke sliding-underline tab bar. Six domains reasoned from the operator's situation, not the data model: **YOU** (profile/notifications/appearance/preferences/shortcuts) · **TEAM** (members/roles) · **COMPANY** (details/task-types[absorbs the Setup launcher]/units) · **FINANCIAL** (billing/automation/expenses/accounting/documents) · **COMMS** (email/templates/lifecycle/portal/client-comms) · **ADVANCED** (map/data-privacy/developer). State in the URL: `?section=<leaf>` canonical (the §2 redirect target + stored deep links), legacy `?tab=` canonicalizes to it; registry `settings-domains.tsx` + `settings-shell.tsx`, thin `page.tsx`. Visibility is granular-permission + company-flag gated (domain shows iff ≥1 section visible; section gated by `isPermissionUnlocked && can` + `canAccessFeature(flag)` + devPermission) — never role names; preserves the `team.view`-only parity the standalone /team had. **Phase-C gating (Jackson's posture ruling): keep Email/Templates/Lifecycle VISIBLE (lead import is ungated/headless) — gate ONLY Client Comms + the autonomy/auto-send/AI-setup panels inside integrations behind `phase_c`** (was Gmail-connection-presence; a normal company never sees autonomy). Live-verified: a non-flagged Maverick sees COMMS = Email/Templates/Lifecycle/Portal, NOT Client Comms. The two **mis-filed "AI" tabs** (invoice + financial settings — verified plain JSONB money-rules, no agent/flag dep) moved OUT of the integrations/`ai` cluster into FINANCIAL › Automation. **Team absorption = parity UNION (master plan §4, zero silent loss):** new `team-section.tsx` carries BOTH the standalone /team page (search · role FilterChips · mailto/tel · last-active · metrics) AND the old settings team-tab (seats/trial · deactivate/reactivate · pending-invite revoke/re-role · RBAC AssignRoleModal · self [YOU] guard · screen-view analytics) — a `// CREW` `InstrumentStrip` (MEMBERS/SEATS+coverage/ACTIVE/PENDING, count-up on the shared curve) over a `RegisterTable` roster, reading identically to Books/Clients/Catalog. **Both unreconciled role paths preserved verbatim** (legacy `users.role` enum via per-row Change role AND RBAC `user_roles` via Assign company role → the only path that clears role_needed notifications) — the data-model divergence is flagged for a future wave, NOT unified here. Verbs behind a shared Radix kebab; one filled-accent CTA. **Registry swap (same commit as the absorption completion):** dropped the `team` route entry (gone from sidebar/palette/shortcuts; keywords folded onto `settings`), middleware 308 `/team`→`/settings?section=team` (param-preserving — `?action=invite`/`?assignRole` carry through), retargeted the unseated-admin lockout flow (CTA + role_needed/reactivation action_urls → `/settings?section=team`/`?section=billing`; the overlay exemption recognizes `/settings`), deleted the standalone page. Live-verified: `/team?action=invite` → `/settings?action=invite&section=team`, Team gone from the rail. **Roles editor FULL rebuild (Jackson-authorized):** 2318-line dnd-kit kanban (blue/amber/emerald tier columns — the off-system "cheap replica") → 926 lines on the kit: `// PRESET ROLES`/`// CUSTOM ROLES` RegisterTables + a per-module **4-option [None|View|Manage|Full] `SegmentControl`** (driven by `detectModuleTier`, tan "Custom" Tag for non-tier mixes) + scope SegmentControl, dirty-aware Save, discard-confirm Back, read-only preset protection, per-role Assigned-members panel. Permission semantics unchanged (`getActionsForTier`/`getPermissionScopes` clamp). Live-verified: list + editor (5 categories, 23 tier segments, members, discard guard) + create→inspect→delete round-trip (test role cleaned from prod). The dropped bulk-add-category (a once-ever power-user shortcut) is the one deliberate descope, per prominence-∝-frequency. **Accounting one-CONNECT (the gate's canonical failure fixed):** the side-by-side QuickBooks+Sage cards → one provider `SegmentControl` + CONNECT, a compact `// ACCOUNTING` live badge (olive CONNECTED Tag + last-sync + Sync now) when connected, settings/disconnect/switch behind a MANAGE `Dialog` (read-only↔full-CRUD with the bidirectional warning, propagate-deletes). Complementary to the sibling P3-4 Books-sync redesign (same pattern, the Books surface). **Conformance:** the 23 surviving tabs onto the kit — hand-rolled pill toggles → `Switch` (12 files), single-select groups → `SegmentControl` (12), raw `<select>` → `Select`, status pills → `Tag` (6), record lists → `RegisterTable`+`RegisterEmpty` (7), search → `SearchInput`, CTAs → `Button`; `// TITLE` section grammar; tokens everywhere (zero Tailwind-palette colors, zero sub-11px, zero raw rgba in tabs, the malformed spaced-`rgba(111, 148, 176,…)` non-rendering bug fixed); accent confined to the one CTA + focus rings; "classify with AI" reworded to behavior (AI-framing rule). Bare hex remains only in sanctioned contexts (Stripe CardElement; color-picker palette data; the portal-PREVIEW client palette). **Dead code:** 8 importer-less files deleted (quick-actions-tab/schedule-settings-tab/email-setup-wizard/filter-funnel-canvas/email-filter-builder/settings-section/task-type-nudge-banner/setup-wizards-tab) — note: my `git rm` of these was swept into the Pipeline lane's `ec54b9d8` via the shared worktree index (harmless — files gone from HEAD). The Phase-C orphans (ai-database-mining/autonomy-status-panel/email-category-autonomy/…) are Calibration/agent building blocks, NOT settings tabs — left intact, only gated at the integrations call site. **Process (§6):** `show_widget` IA + Team mock to Jackson pre-code (he chose horizontal over the left-rail); skills `custom-skills:interface-design` (IA + recomposition), `ops-copywriter` (en+es labels — terse/tactical, "Comms" not "Communications", behavior over AI), `custom-skills:audit-design-system` done-gate. **Done-gate `audit-design-system` over all touched files — ZERO high-severity findings** (Switch×12/SegmentControl×12/RegisterTable×7/Tag×6/Select×2 adopted; zero palette colors/sub-11px/accent-misuse/off-token-radius/raw-rgba; sanctioned bare hex only). `npx tsc --noEmit` **0 errors project-wide**; `eslint` **0 errors** (remaining warnings all pre-existing in Phase-C/wizard files). **Live-verify:** IA shell + Team section (kebab/filters/AssignRoleModal/redirect/gating) + Roles (list+editor) verified on `overhaul-shell` 3017 as Pete on real Maverick data; accounting + per-tab conformance are tsc/eslint/audit-clean — the shared dev renderer repeatedly hung under multi-lane recompile load (the documented 3017 instability) so per-tab screenshots were deferred, not all captured. A recurring `async_hooks` webpack warning (supabase/helpers→auth-store→providers) is pre-existing, not from this wave. **Concurrency (3 lanes co-edited this worktree/branch — Settings P3-6 + Pipeline P3-7 + Books-sync P3-4):** all my files staged BY NAME in 7 atomic commits; the sibling lanes' files (pipeline/*, books/sync/*, `tailwind.config.ts` cake-tokens, `button.tsx`) never touched; my commits verified foreign-file-free; the P3-7-1 lane layered its Cake-token commits on top of my `9b674fed`. Commits `9f08e478` (IA shell) · `b23c56d2` (Team section) · `2db645c7` (retire /team) · `d5008ec9` (i18n) · `c3213861` (Roles rebuild) · `b0299497` (Accounting) · `9b674fed` (conformance) — staged by name, **NOT pushed**, no AI attribution. | Jackson (horizontal IA / keep email visible / full Roles rebuild) / Session P3-6 |
| 2026-06-15 | **P3.5 Projects map-absorption shipped — the standalone Leaflet /map is now the Projects MAP view mode, on Mapbox GL.** Four Jackson rulings up front (OPS-faithful `show_widget` mock of the view + the parity inventory reviewed pre-code): **(1) engine = unify on Mapbox GL** (react-map-gl) — the old /map was Leaflet, a *different* engine than the project workspace window's Mapbox; the new view reuses the workspace's round-glow `ProjectPin` + `dark-v11` + `NEXT_PUBLIC_MAPBOX_TOKEN`, so clicking a pin opens the workspace with the SAME pin (the whole argument for unification); **(2) crew plotting DEFERRED** (documented, not silent loss) — the old /map plotted projects only; `crew_locations` exists but has **0 live rows** on prod + no web writer, so a layer would render empty; `mapShowCrewLabels` stays an inert pref until crews broadcast; **(3) honor `mapDefaultZoom` only, NOT traffic** (Jackson: "don't need traffic"); **(4) layout = Variant 4** — collapsible-to-full-bleed left SITES drawer. **Feature parity (read /map top-to-bottom, zero silent loss):** status-colored pins · location-grouped stacked pins w/ count badge · fit-bounds · custom zoom/recenter toolbar (mirrors the workspace `MapToolbar`) · status `FilterChips` · search (reuses the surface-wide HUD filters — no duplication) · pinned/total count · status legend · per-row "open in Google Maps" — and the load-bearing parity call: **19% of projects (56/298) are un-geocoded and have NO pin**, so the drawer's dedicated **NO-GPS group** is parity (a pins-only map would silently drop them) and lets the owner open one to add an address. **Interaction model (§4):** pin click → `openProjectWindow` (never a nav); drawer pinned-row → locate (fly-to); no-GPS row → open window. **View-mode integration:** union `canvas|spreadsheet` → **`+map`**; switcher = the shared `SegmentControl` (MAP segment gated on `map.view`); `?view=` is the source of truth when present (the redirect target + shareable), localStorage holds the default, only explicit toggles persist; map mode renders a top-bar layout (map metrics via `useMapMetrics` + the workbar) over the full-bleed Mapbox view, matching the table-v2 pattern. **Registry swap + redirect + page retirement (mirrors the Team absorption, same theme):** dropped the `map` registry entry (gone from sidebar/palette/number-shortcuts, all registry-derived; keywords map/locations/tracking/gps folded onto `projects`); middleware **308** `/map`(+`/map/*`)→`/projects?view=map`, param-preserving; deleted the standalone page **and** the orphaned Leaflet `ProjectMap` (its only consumer) — the dashboard background map keeps its own Leaflet (separate surface, out of scope). **Conformance (§6 punch list):** the two named Projects divergences — projects table-header `tracking-wider`→`tracking-[0.16em]`; and progress bars rendering **rose at 100%** ("completion reading as a negative color") fixed via a new `projectProgressColor()` (status-tinted in flight, **olive `#9DB582`** once full/Completed) across the canvas card + legacy spreadsheet row + table-v2 progress cell — **WITHOUT** touching the iOS-synced global `PROJECT_STATUS_COLORS` (pins/dots/tags unchanged). The audit's surface-wide `tracking-wider` sweep is the brief-scoped table-header only; the rest is a documented separate conformance pass. **Process (§6):** Variant-4 dark mock + parity inventory + the four rulings to Jackson pre-code; skills — `custom-skills:interface-design` (IA + map-view recomposition) · `custom-skills:wireframe` (4 layout variants) · `animation-studio:animation-architect` (pin pulse = the existing `animate-pin-pulse` keyframe; view cross-fade/fit-bounds = the single OPS curve; reduced-motion aware; web = no haptics) · `ops-copywriter` (en+es `map.*` — terse/tactical, `//` titles) · `custom-skills:audit-design-system` done-gate. **Done-gate `audit-design-system` over all 7 new/changed UI files — zero high findings after remediation** (it caught + I fixed: 3 sub-11px micro-labels lifted to the 11px floor; the chevron's accent left-border + the selected-row accent inset bar removed → **accent confined to focus rings only**, neutral active treatment like SegmentControl/FilterChips; ease-smooth added to roster transitions; dead bare-hex status fallbacks removed). `npx tsc --noEmit` **0 errors project-wide**; `eslint` **0 errors** over all 13 wave .ts/.tsx files (the 4 warnings are pre-existing in spreadsheet-row — unused import identical on HEAD + image-gallery `<img>`s). **Live-verify (own `dev:webpack` server, port 3017, fresh `.next`, dev-bypass as Nick on real Maverick data — 23 projects, 21 mapped / 9 missing — 3017 was free, never contended):** map view renders (drawer / `CANVAS·SPREADSHEET·MAP` switcher / map metrics / status chips / roster / the 9-row NO-GPS group); `/map?foo=bar` → **308** → `/projects?foo=bar&view=map` (param-preserving, curl-confirmed); switcher navigates + `?view=` URL contract; **Map gone from the sidebar** (registry-derived, `mapRouteInNav:false`); `map.view` gate positive (Nick has it → MAP shows); **workspace window opens from the map view** — clicking a NO-GPS row opened `// QUICKBOOKS ESTIMATE QB-220 · VIEWING` (the identical `openProjectWindow` callback a pin click uses), fittingly an un-geocoded job reachable only via the drawer. Screenshots: map-view · window-open. **Documented limitation (not a defect, surfaced to Jackson):** the Mapbox **tile+pin** rendering can't be screenshot-verified in local dev — `NEXT_PUBLIC_MAPBOX_TOKEN` is empty in every local `.env.local` (the existing workspace map has the identical limitation); the token-missing fallback renders correctly and the pin/fit-bounds/pin-click paths are review-verified; renders in production where the token is set. **Concurrency:** the primary OPS-Web checkout holds uncommitted sibling WIP on the same table-v2 files (the P2-superseded edge-tab refactor) — a DIFFERENT checkout; this worktree stayed clean throughout, all 15 files staged BY NAME across 6 atomic commits, no sibling WIP touched. Commits `63fbd982` (map components + i18n) · `8399c820` (view-mode integration + ?view contract) · `ed3ce37c` (registry + 308 redirect) · `98404e84` (progress-color + tracking conformance) · `6b849e24` (audit-gate polish) · `62645ec0` (retire /map page + Leaflet component) — staged by name, **NOT pushed**, no AI attribution. | Jackson (4 rulings: Mapbox / defer crew / no traffic / Variant 4) / Session P3-8 |
| 2026-06-15 | **P3-9 Schedule rename (Calendar→Schedule internals) + conformance shipped — the surface now matches its user-facing label end-to-end.** Route + visible labels landed in P2; this wave finished the INTERNAL rename + the §6-Schedule conformance gaps. **Internal rename (client-internal, non-DB-coupled only):** `CalendarPage`→`SchedulePage`; `Calendar{Header,Toolbar,DndShell,GridDay}`→`Schedule*` + the dnd-shell's internal `Calendar{DragState,ResizeAPI,ResizeContext,DndShellProps}`; `useCalendarResize(Context)`/`useCalendarDragState`→`useSchedule*`; `useCalendarStore`(+`CalendarStoreState`)→`useScheduleStore`; `InternalCalendarEvent`→`InternalScheduleEvent`; `useCalendarMetrics`→`useScheduleMetrics`; `calendar{View,Event}Variants`/`SPRING_CALENDAR_DRAG`→`schedule*`/`SPRING_SCHEDULE_DRAG`; files `stores/calendar-store`+`lib/utils/calendar-{utils,constants}`+route-local `calendar-*.tsx`/`use-calendar-resize`→`schedule-*`; `tests/unit/calendar`→`tests/unit/schedule`; analytics `trackScreenView("calendar")`→`"schedule"`; the `data-*-motion-scope` CSS attr + the realtime channel name; MetricsHeader `tabId` "calendar"→"schedule" (matches projects/map/pipeline; client-only localStorage visibility key). **i18n:** the live surface dict `calendar.json`→`schedule.json` (en+es, byte-identical — no displayed copy changed); the dead zero-consumer "Job Board" `schedule.json` that squatted the namespace removed; `DictionaryNamespace` drops `'calendar'`; both `useDictionary("calendar")` consumers→`"schedule"`; Settings shortcut label "Calendar"/"Calendario"→"Schedule"/"Agenda" (= `nav.schedule`, ops-copywriter blessed). **Deliberately LEFT (DB/API/iOS-coupled or genuine calendar-domain, documented):** `calendar.{view,edit,create,delete}` RBAC permission strings (DB `role_permissions` + iOS task-scheduling gate); the `calendar_events`/`calendar_user_events` DB facades (`calendar-service`/`calendar-user-event-service`) + their data hook + `queryKeys.calendar` cache namespace; the `CalendarScheduler` date-picker widget (used by task forms, not this surface); `CalendarEvent`/`CalendarUserEvent` model types; the `"ops-calendar"` zustand persist key (preserves users' stored view+filters; the persist-migration test still asserts on it). **Conformance:** the 3 named legacy easings (`day-timeoff-card`/`day-personal-event-card` `"easeOut"`, `cascade/ghost-overlay` `"easeInOut"`) **+ a 4th** (`cascade-confirm-bar` banned spring) → `EASE_SMOOTH`, each gaining `useReducedMotion()` fallbacks (the ghost pulse goes static under reduced-motion — the CSS motion-scope block can't reach Framer's JS loop); `tracking-wider/widest`→`tracking-[0.16em]` (62 sites); **accent discipline restored to today-tick + CTA + focus only** (toolbar filtered-row tints + filter-count, month-picker selected state, the page grid-bg's #417394 leak → neutral; hardcoded `#6F94B0` CTAs/now-lines → `var(--ops-accent[-line/-soft])`; the no-photo crew avatar's deprecated `#59779F` ring → neutral); exact-token hex → tokens (`#93321A`→brick, `#C4A868`→tan, `#95B07A`→status-task-completed, `#A5B368/#9DB582`→olive, `#6A6A6A`→text-mute, `#999999/#666666`→text-3, tan rgba→tan-soft/-line, `rgba(255,255,255,0.04)`→surface-input); sub-11px labels→11px floor; cascade warning icon accent→tan (caution semantic), APPLY text→black-on-accent, button radii 3→5. **Task-card chip-overlap fix (the one design-judgment change — `show_widget` mock approved by Jackson, "trailing chips"):** the day task-card's absolute top-right type chip + bottom-right status chip moved into normal flow as trailing `shrink-0` siblings (type ends the title row, which now `flex-1 min-w-0` truncates BEFORE it; status joins the bottom row's time group), so chips can never overlap the title/time — top-right/bottom-right reading + full iOS-parity info preserved, nothing dropped. **Skills:** `animation-studio:animation-architect`+`web-animations` (easings), `custom-skills:interface-design` (card recomposition), `ops-copywriter:ops-copywriter` (shortcut label en+es), `custom-skills:audit-design-system` (done-gate). **Done-gate `audit-design-system` over all touched files — zero unsanctioned high findings** (0 tracking-wider / 0 legacy easings / 0 selected-state accent tints / 0 off-ladder radius; every surviving `var(--ops-accent)` is today-tick/CTA/focus/now-line). **Documented residuals (NOT regressions, deferred):** es/schedule.json key-parity gap (pre-existing → P4 i18n sweep); inline `fontSize:9/10` dense-calendar micro-text in month-bars/crew-blocks/hover-popover/context-menu (pre-existing 11px-floor gap, not in §6 Schedule scope; deferred to the typography sweep alongside the Cake-scale migration to avoid clipping tight cells); off-scale Cake title sizes (15/12px — the explicitly-deferred P3-7-1 Cake-scale migration); inline dark-panel surfaces (#141414/#0D0D0D/#1A1A1A — Tailwind tokens exist but no inline CSS var; className refactor deferred); the shared legacy `SegmentedPicker` view switcher (audit §5.6 class-B debt, shared w/ dashboard — the praised `// DAY/WEEK/MONTH/CREW` grammar would regress under the generic SegmentControl); data-default task-color fallbacks `?? "#6F94B0"/"#59779F"` (parsed as hex downstream); intentional #FFFFFF max-contrast event titles; the pre-existing AUTO-SCHEDULE button stub. **Verify:** vitest **61/61** (incl. the persist-migration test asserting on the kept `ops-calendar` key + the `calendar.edit` edit-scope test); `npx tsc --noEmit` **0**; `eslint` **0 errors** (11 pre-existing warnings — unused vars/`<img>`/exhaustive-deps/AUTO-SCHEDULE console). **Live-verify** (own `dev:webpack` overhaul-shell server, port 3017, dev-bypass as Pete on real Maverick data): all 4 views render/switch (day empty-today / week + crew `EASE_SMOOTH` cross-fade / month task-bars + accent today-tick on the 15th); rename live (`data-schedule-motion-scope` present, dict-resolved `// DAY/WEEK/MONTH/CREW`, top-bar "SCHEDULE"); **card fix confirmed on 11 real cards — `noOverlap:true`, title `flex-1`+truncate, trailing type chip** ([Paving]/[Installation]/[Inspection]); console errors = the pre-existing `create_notification_if_new` RPC permission-denied only. Screenshots: day / week / month / crew. **Concurrency:** tree clean throughout (re-checked before each git mv/stage); all files staged BY NAME across 5 atomic commits; the cross-surface touches (settings shortcut label, dashboard `InternalScheduleEvent` import, motion.ts variant rename, the 3 lib hooks) were mine-only at stage time. Commits `5ef45cda` (internal rename) · `a100a208` (dict namespace) · `bd971c60` (easings) · `bd4892e6` (tracking/sub-11px/color/accent) · `1e8440b1` (card-overlap fix) — staged by name, **NOT pushed**, no AI attribution. | Jackson (trailing-chips mock approved) / Session P3-9 |
| 2026-06-22 | **Dashboard conformance/polish pass landed (P3.7 §6 "light polish" — Dashboard punch list; user-approved widget-grid baseline preserved, no restructure).** The §6 Dashboard gaps closed without touching the grid/widget concept. **`// TITLE` widget-header grammar (the headline gap):** minted a shared `WidgetTitle` primitive (`widgets/shared/widget-title.tsx`, barrel-exported) rendering DESIGN.md §10's header voice — JetBrains-Mono 11px uppercase, muted `// ` slash, `tracking-[0.16em]`, `text-text-3` — identical to InstrumentStrip / the P3-3 drawer-panel headers; threaded it through **every** widget across **all** tiers (loading / empty / xs / sm / md / lg), **120 usages**, replacing the ad-hoc `REVENUE`/`RECEIVABLES`/… spans (gallery 01). Loading states that hand-rolled `CardHeader/CardTitle` (booking-rate, win-rate, lead-sources, pipeline-funnel, todays-schedule) collapsed to the canonical plain-div + `WidgetTitle` pattern (unused Card-subcomponent imports trimmed). **Tracking:** `tracking-wider/widest`→canonical `tracking-[0.16em]` (audit §6 "universal micro-label divergence"; 163 sites, all confirmed mono micro-labels — zero on Cake/Mohave). **A/R aging CURRENT (§5.5):** confirmed already olive (P4-1-5) AND **runtime-verified** the leak does not recur — with demo operator Pete's customisable accent resolving to `rgb(65 115 148)` (`#417394`), the CURRENT bar resolves to `rgb(157 181 130)` (olive `#9DB582`), i.e. a data bar does **not** resolve to the accent (the §5.5 lesson: a hardcoded check is insufficient; verified `WT.current`→`--color-financial-current`, not `WT.accent`). **Accent discipline (§7 — accent only on primary CTA + focus):** CUSTOMIZE/DONE header toggle de-accented (dropped `bg-ops-accent text-white` → neutral `Button` `default` active state — accent never on toggles, and accent fill pairs with black not white); the widget-tray Done/Cancel hand-rolled buttons → `Button` primitives (Done = `primary` = the one legitimate customize-mode commit CTA, fixing text-white→black / Mohave→Cake / radius 4→5; Cancel = `secondary`); spacer 8-handle resize affordances no longer flash the steel-blue accent on drag (neutral white brightening — accent never on icons; was a static `#6F94B0`/`rgba(111,148,176,…)`, not even the runtime accent); phase-c autonomy `auto_draft` ramp colour de-accented to bright-neutral `var(--text-2)` (passive grays → bright-neutral "prepares, waits" → earth-tone "acts"), removing accent from its data bars/cells. **Accent-on-data-viz RULING (the crux judgment, design-skill-grounded):** the deliberate `WT.accent` *featured-colour / status* data-viz across ~15 widgets (top-clients rank bar, lead-sources/expense first series, booking sparkline, Sent/Pending/Info status dots, etc.) is **sanctioned, NOT a leak, and was NOT stripped** — the widget-builder skill's own Status-Badges table assigns accent to Sent/Pending/Info, and audit §6 explicitly classified dashboard "accent confined to `WT.accent` data-viz" as **"Already cohesive."** The only genuine accent-leak was the aging *semantic ramp* (a healthy/neutral value defaulting to the runtime accent), already fixed → olive; stripping the sanctioned featured-colour usage would be a redesign this "light polish" forbids. **Token hygiene:** all exact-token raw rgba → tokens (`surface-hover`/`fill-neutral-dim`/`surface-input`/`border`/`border-medium`) across widgets + shared primitives + tray chrome; phase-c earth-tone hexes → `var(--olive/--tan/--text-*)` + `color-mix` (the `${tone}33/0F` alpha-suffix rewritten); `PlaceholderWidget` + drag-preview `rounded-lg/md`→`rounded-panel`, drag-preview surface→`bg-glass-dense`/`border-border-medium`. **11px floor:** `text-[9/10px]`→`text-micro`, Cake 10px→`text-cake-badge` (keeps weight 300). **Method:** shared primitive first → Phase-A `tracking` find-and-replace → a verified parallel **Workflow** over the 27 data widgets (apply + adversarial-verify per file; `behaviorPreserved:false` count = **0**, 7 tsc-clean confirmations, zero wrong conversions; it surfaced 2 residual loading-titles + the phase-c/spacer accents which I then fixed by hand) → judgment files (page/tray/shell/spacer/phase-c) hand-done. **Skills:** `custom-skills:widget-builder` (HUD widget spec) · `custom-skills:interface-design` (the accent-on-data ruling + tray-CTA reasoning) · `frontend-design:frontend-design` · `custom-skills:audit-design-system` (done-gate) · `animation-studio:animation-architect` — **no new motion authored** (audit confirms dashboard easings already canonical with reduced-motion intact; the animation hook is a false-positive for a token pass — all motion preserved). **Done-gate `audit-design-system` over all touched files — zero high findings:** 0 `tracking-wider/widest`, 0 `#6F94B0`, 0 `bg-ops-accent text-white`, 0 exact-token className/border rgba, 0 bare hex in widgets (the lone `#EDEDED` is a Framer color-animation concrete value, which can't interpolate a `var()`); non-exact rgba (inline glows/gradients/separators) left as acceptable-unspecified. `npx tsc --noEmit` **0 errors**; `eslint` **0 errors** (38 pre-existing unused-var/exhaustive-deps **warnings** — same on HEAD, e.g. `heroClass`/`showDetail`/`config`; my Card-subcomponent import trims are clean). **Live-verify** (own isolated `dev:webpack` server **port 3030**, NOT the shared 3017; dev-bypass as Pete on real Maverick data): before/after/customize/final screenshots — every widget header now reads `// REVENUE`/`// RECEIVABLES`/`// EXPENSES`/`// PROFIT MARGIN` (even the tray catalog previews); CURRENT bar olive (runtime accent ≠ bar colour, asserted via `getComputedStyle`); CUSTOMIZE neutral; **customize round-trip exercised** (entered → tray opens with detents/size-pills/gap-selector/widget-catalog → tray DONE = accent `primary`, header DONE neutral → commit+exit clean), no behaviour regressed; 0 console errors on load (the 2 that appear post-render are the pre-existing Gmail review-items timeout + `create_notification_if_new` RPC permission-denied under the dev-bypass anon role — unrelated). **Concurrency:** ran alongside the Schedule polish lane on the SAME `feat/web-overhaul` branch + worktree; all changes confined to `src/components/dashboard/**` + `src/app/(dashboard)/dashboard/page.tsx` (disjoint from Schedule's calendar/schedule files); tree was clean at session start, `git status` re-checked before each commit, staged BY NAME. Commits `80291810` (WidgetTitle primitive + barrel) · `edb87316` (widget-set conformance — 37 files) · `f36b77c3` (shell/tray/page chrome conformance) — **NOT pushed**, no AI attribution. | Session — Dashboard conformance (P3.7) |
| 2026-06-22 | **P3-8 Schedule conformance/polish pass — verified the surface already meets the cohesion standard; landed one residual 11px-floor fix.** This spawn was effectively a re-issue of work the **P3-9 wave (2026-06-15, `5ef45cda…1e8440b1`) already completed comprehensively** (rename, 4 legacy easings→`EASE_SMOOTH`+reduced-motion, `tracking-wider/widest`→`tracking-[0.16em]` ×62, accent→today-tick/CTA/focus, exact-token hex, sub-11px labels, the trailing-chips card-overlap fix — audited zero-high, vitest 61/61, live-verified day/week/month/crew). **Independently re-confirmed it holds in the current tree** via exhaustive grep across every conformance dimension (**0** legacy easings / `easeOut`/`easeInOut`/spring, **0** `tracking-wider/widest`, **0** `@carbon/icons-react`, **0** `.glass-subtle`, **0** elevation box-shadows — the month today-cell `box-shadow: inset 0 0 0 1px var(--ops-accent-line)` is an intentional 1px **border-emulation** on the sanctioned today-tick accent, the cell already owning its grid right-border, not a depth shadow) + first-hand source reads of the load-bearing card/toolbar/popover files. **One genuine residual found & fixed:** the team-filter image-less avatar **monogram** in `schedule-toolbar.tsx:372` rendered `text-[9px]` — the lone sub-11px **className** P3-9 left (distinct from its documented inline-`fontSize:9/10` dense-micro-text deferrals; the toolbar is not in that deferred file set) → bumped to the **11px floor** + `leading-none` (matches the canonical `AvatarFallback`'s 11px monogram and the row's adjacent `text-[11px]` name label; the bespoke 18px micro-avatar itself stays — cohesion-audit §4 sanctions "hand-rolled avatars in tight contexts"; pure token conformance, no layout/behavior change; lives in a conditional fallback branch so it only paints for an avatar-less member). **Everything else categorized, NOT a gap:** all remaining inline `fontSize:9/10` (month-bars/crew-blocks/hover-popover/context-menu), dark-panel `#141414/#0D0D0D/#1A1A1A`, `?? "#6F94B0"/"#59779F"` color-parse fallbacks, off-scale Cake 15/12px, and `#000/#FFFFFF` max-contrast are **P3-9-documented deliberate deferrals**; `#D99A3E` in `filter-sidebar` is the **system-wide active-status amber** (40+ sites — `PROJECT_/TASK_STATUS_COLORS`, pipeline/models types, map pins, portal badges), so converting only Schedule's instance to a non-existent token would break that convention → out of Schedule scope. Internals correctly left "calendar" (`calendar.view` perm, `calendar` i18n namespace, `calendar_events`/`calendar_user_events` services, `queryKeys.calendar`) per DB/iOS coupling — untouched. **Skills:** `custom-skills:interface-design` (avatar-monogram judgment — keep bespoke micro-avatar, raise only the monogram to the floor), `custom-skills:audit-design-system` (done-gate over the touched file → **zero high**; one INFO = the established `text-[Npx]` arbitrary-value convention, pre-existing codebase-wide). ops-copywriter n/a (no strings changed); animation-architect n/a (no motion authored — the auto-injected `web-animations` skill is a false-positive). **Verify:** `npx tsc --noEmit` **0 errors project-wide**; `eslint` clean on the touched file; an isolated **detached-worktree** server (`/tmp/ops-sched-verify` at HEAD, own `.next`, port 3019) booted and served 200 (middleware+login compiled; `/schedule` 307s at the client-side dev-bypass auth gate, as expected). The full browser screenshot ceremony (login-bypass → 4 view-switches → Books cohesion flip) was judged **disproportionate** for a CSS floor-bump in a conditional branch on a surface P3-9 already live-verified across all 4 views — and the env was pathologically slow under parallel load (92s/route) — so verification relied on the code-grounded checks + P3-9's logged 4-view live evidence; offered Jackson a fresh isolated cohesion-flip session on request. **Concurrency discipline:** did **NOT** edit the actively-mutating shared `.claude/launch.json` (it was rewritten mid-session by a sibling — used my own isolated server instead); did **NOT** touch the Dashboard sibling lane's uncommitted WIP (`components/dashboard/widgets/shared/index.ts`, `widget-title.tsx`); staged my one file BY NAME. Commit `64c1481e` (schedule-toolbar.tsx) — **NOT pushed**, no AI attribution. | Session P3-8 |
| 2026-06-22 | **P4 cross-cutting sweep complete — verdict CONDITIONALLY CLEAN, proceed to P5 with a documented flag punch-list.** Ran the §7 checklist across all 8 surfaces + shell as 14 parallel read-only finders seeded with the decision-log / P4-1 / P4-2 do-not-reflag brief; fixed every mechanical + unambiguous failure in 12 atomic commits (62 files; `tsc --noEmit` clean, `next lint` zero-error on all 51 touched files): `/expenses`→Books redirect (`06fef74d`), es/schedule +19 & es/dashboard +17 parity + drop 9 dead `commandPalette.nav` keys + product-editor i18n (`079811c1`), strip `!`/`Welcome aboard` copy en+es (`33552e2b`), mono/tabular/em-dash card numbers (`7a961dc1`, `8ff779cb`), open-workspace-window from 4 dashboard widgets + the Schedule task-card (`7f1dfd1b`), confine accent — neutralize shared `Progress` + `Badge.info` + sidebar links + selection checkmarks + pipeline banner/status chips (`75c3157a`, `c065d441`), earth-tone/radius → tokens (`724274b2`), off-curve easings → EASE_SMOOTH + add reduced-motion snaps (`40391fea`), tactical skeletons + RegisterEmpty for loading/empty (`360f5780`), delete dead `.ops-badge`/`.ops-card-accent`/`--glass-bg-subtle` + table-v2 Cake-12 → cake-button (`7f325f4b`), pipeline decorative `z-[30]`→`z-[5]` (`c065d441`). **Schedule rename safety = clean PASS** (`calendar.view`/`calendar_events`/`calendar_user_events`/`queryKeys.calendar`/`"ops-calendar"` all verified intact — the rename broke nothing DB/iOS-coupled). **FLAGGED for Jackson (not silently changed):** (1) Schedule never consumes `?date`/`?task` deep-links → a live recurrence cron's `/schedule?date=&task=` notifications land on today's view (functional); (2) the Settings email-import wizard (ungated, live) still hardcodes ~40 `#6F94B0` + non-spec hex → a dedicated P4-2-class convergence wave; (3) legacy role-name gates — inventory items-tab + lockout-resolver gate UI on `selectIsAdminOrOwner`, plus 8 banned `.in("role",[…])` recipient-resolvers → migrate to granular permissions; plus i18n dead-namespace deletions (`sidebar`/`breadcrumbs`/`pmf`/`bug-reports`), Schedule side-panel ~26 un-i18n'd literals, the 340-site `rounded-[Npx]`→named-token codemod (DEFER), legacy `ops/` `shadow-xl` + status-glow rulings, and ModePill/picker accent-judgment calls. Report: `docs/audits/2026-06-13-p4-cross-cutting-sweep.md`. **NOT pushed**, no AI attribution. | Session P4-3 |
| 2026-06-24 | **P4-5 — legacy role-name permission gates RESOLVED + correctness-verified (closes P4-3 flag #3, the §11 "FAIL → flagged" dimension). Zero real-customer access regression.** The migration itself was **already implemented** across 3 commits authored 2026-06-23 — this pass was an **independent rigorous verification** of all four non-negotiable correctness requirements + this log closure; **no code re-committed** (the only new artifact is this entry). **What landed (verified present + correct):** (a) **client UI gates** (`c1f4fe59`) — inventory items-tab write actions (apply-tags / bulk-delete / per-row edit+delete) `selectIsAdminOrOwner` → `can("inventory.manage")`; lockout-resolver **and** lockout-overlay (who may clear a subscription/seat lockout) → `can("settings.billing")` with a deliberate **fail-safe** `canResolveLockout = !permsReady || canManageBilling` (the permission store is async + non-persisted, so an admin never loses the `/settings` recovery path during the hydrate flash); the Connect-Gmail action-prompt (`useActionPrompts`) `selectIsAdmin` → `can("settings.integrations")`, with the one-shot eval now gated behind `selectPermissionsReady`; the dead role-NAME selectors `selectIsAdmin`/`selectIsAdminOrOwner`/`selectIsFieldRole` **deleted at source** (`auth-store.ts` + `store/index.ts` barrel) so the anti-pattern can't be reached again. (b) **task assignable-members fallback** (`701d0dca`) — task-list keyed off `m.role === Admin/Owner` → company **management list** (`admin_ids ∪ account_holder`). (c) **8 server recipient-resolvers** (`65008e70`) — the banned `.in("role",["admin","owner"])` in crons (appointment-reminders, auto-confirm-schedules) + services (payment-reminder, project-lifecycle, task, approval-queue, invoice-suggestion, autonomy-milestone) → shared `getCompanyManagerUserIds(supabase, companyId)` = `account_holder_id ∪ admin_ids` (deduped, text[]/CSV-tolerant). **Req-1 (right key per gate):** read every call site — inventory-write→`inventory.manage`, lockout/billing-recovery→`settings.billing`, integration-connect→`settings.integrations`, notification recipients→the intentional management set (not a role name). **Req-2 (NO access regression — DB-verified, prod `ops-app`):** preset **Admin** and **Owner** roles grant **all three** keys in `role_permissions` (Office grants only `inventory.manage`; Operator/Crew/Unassigned none); the permission-store **bypass** (`fetchPermissions`: account_holder OR member of `admin_ids` → full `ALL_PERMISSIONS`@`all`) covers every real owner/admin. Regression sweep over all 54 `role∈{owner,admin}` users: each real-customer account **either hits the bypass or holds a role assignment granting the key** — the only 30 that would lose the gated UI are **entirely synthetic personas in "PERSONA TEST POOL"** (trial co, NULL account_holder, empty admin_ids, no `user_roles` row); **zero real customers**. Live-verify co MAVERICK: Pete (owner = account_holder + admin) → bypass → passes every gate; Mike (Office) now **gains** inventory-write (intended granular expansion — Office was denied under the old admin/owner gate, an expansion not a regression); Tom (Operator)/Nick (Crew) correctly denied, unchanged. (Trap noted for re-runs: `account_holder_id` is nullable text → SQL `(holder = id)` is NULL not false when unset; must `coalesce(...,false)` to mirror the client's JS `===`/`.includes()`.) **Req-3 (client-catalog registration):** all three keys already present in `src/lib/types/permissions.ts` (`inventory.manage`, `settings.billing`, `settings.integrations`) — both the DB grant AND the `ALL_PERMISSIONS` catalog exist, so the account-holder/company-admin derive-from-catalog path can't silently deny. **Req-4 (RLS / app-role):** the resolvers read `companies` via the **service-role** client (`getServiceRoleClient()` in crons, `setSupabaseOverride`) — RLS-bypassed, the same privileged context the old `users` role query already ran in; reading `companies(account_holder_id, admin_ids)` is no more restrictive than the prior read, and unconfigured companies (empty managers) keep a **non-role final fallback** (crons fall back to any company user → no notification-recipient regression). **Residual role refs confirmed legitimate (NOT gates, out of scope):** `user-service.fetchUsers` optional `role` query-filter param (no admin/owner caller), `schedule/page` `role !== Unassigned` timeline candidate filter, `team-section` role **display labels**. **Verify:** `npx tsc --noEmit` **0 errors project-wide**; `eslint` **0 errors** across all 17 touched files (15 pre-existing warnings only — console/unused-var, none at migration lines); fresh grep confirms **zero** role-name gating remains at any touched site. Skill: `supabase` (read-only prod role_permissions / bypass / regression-population queries). **NOT pushed**, no AI attribution. | Session P4-5 |
| 2026-06-24 | **P4-4 — Settings email-import wizard: determined CONVERGE (not GATE), then closed the residual token-hygiene tail (closes P4-3 flag #2).** **STEP-1 ruling = CONVERGE.** The wizard (`import-pipeline-wizard.tsx` + `wizard-steps/*`) is the **deliberately-ungated email lead-import surface**, not the shelved Inbox UI and not a Phase-C agent feature: `integrations-tab.tsx` renders for **all** companies and draws the gate line *inside* itself — the genuinely Phase-C panels (`AutonomyStatusPanel`, `AutoSendSettings`, `AiSetupCard`) are each gated behind `canAccessFeature("phase_c")`, while the lead-import wizard + Gmail/M365 connect are intentionally open (matches the 2026-06-11 dark-launch decision "lead import ungated"; master plan §4 gates *Inbox / Calibration / Agent Queue* specifically, none of which is this Settings→Integrations wizard). Gating it would regress a live setup flow for every non-Canpro company → wrong. **Discovery: the structural + i18n + voice + radius convergence had already landed 2026-06-23** — `0d8f53b4` (converge onto the design system, 16 files / 494 deletions), `e701c05b` (i18n en+es), `bdc6ab7d` (radius codemod) — i.e. **one day after** the P4-3 sweep was written (2026-06-22), so flag #2's "~40 `#6F94B0`" evidence was already stale. Re-verified in the current tree: **zero** `#6F94B0` literals remain (only explanatory comments), `#FF6B4A`/`#1a1a1a`/off-ladder grays gone, wizard fully i18n'd, radius codemod-clean, no spring/bounce, EASE consistent. The wizard is a **modal flow archetype** (stepper rail + lead-review carousels + triage cards), not a register surface, so RegisterTable/SegmentControl/FilterChips don't apply — it correctly consumes `button`/`key-hint`/`dialog` from the kit. **My residual token-hygiene closure (the computed/inline-style tail the convergence left), 8 files:** (a) `confirm-pipeline-step` STAGE_CONFIG now **sources canonical `OPPORTUNITY_STAGE_COLORS`** from `@/lib/types/pipeline` instead of a local re-declaration — fixes 2 divergent swatches (`lost` `#8A8A8A`→`#6B7280`, `discarded` `#6A6A6A`→`#444444`) so wizard stage colors are identical to the pipeline board/table everywhere, plus dedup; (b) inline-style bare-hex that equalled tokens (text ladder `#EDEDED/#B5B5B5/#8A8A8A/#6A6A6A` + earth tones `#9DB582/#C4A868/#B58289`) → `var(--text*/--olive/--tan/--rose)` across analyze/triage/consolidate/filter (verified consumed as plain inline style / decision-badge `style`, **not** framer `animate` targets); (c) rgba literals → tokens — card-carousel peek bg → `var(--surface-vignette)`, confirm-sources icon-tile → `bg-surface-hover`, activate interval-pill → `border-border-medium`, and analyze's stage-indicator state colors → canonical **olive-soft/olive-line + surface-active/border-medium** classes (olive state pinned to the 12%/30% earth-tone ladder the rest of the app uses); (d) triage badge's `${color}30` hex-alpha border → `color-mix(in srgb, var(--token) 19%, transparent)` (matches the dashboard-widget idiom); (e) **accent confinement** — the four persistent (non-focus) inline-edit underlines `border-b border-ops-accent` (triage/confirm-pipeline/consolidate ×2) → neutral-resting `border-border-medium` + `focus:border-ops-accent`, killing the flag's core concern (a non-CTA element persistently resolving to the user's **runtime** accent) and matching the canonical Catalog active-input pattern (inputs autofocus, so the accent cue is preserved on focus). **One documented exception:** `analyze-step` `barColor` stays a concrete literal — it's a framer-motion `animate` interpolation target and CSS `var()` can't be tweened (olive literal === `--olive`). **Done-gate `audit-design-system` over the 8 touched files → zero high** (every remaining value traces to a token or is the documented animated/scrim exception; the only non-token color left is that one animated bar + the `rgba(0,0,0,0.7)` modal scrim). **Verify:** `npx tsc --noEmit` **0 errors project-wide**; `eslint` **0 errors** on all 8 touched files (2 pre-existing unused-var **warnings** — `Plus`, `removeContactFromGroup` — present before this pass, not at my edits); isolated **`dev:webpack` port 3030** (NOT shared 3017), dev-bypass as Pete on Maverick — wizard **opens cleanly** at CONNECT with the full CONNECT/SCAN/SOURCES/REVIEW/ACTIVATE stepper, forcing runtime compile of the entire statically-imported wizard-steps tree (incl. the new `OPPORTUNITY_STAGE_COLORS` import + `color-mix`), **zero** relevant console errors (only the pre-existing `create_notification_if_new` permission-denied under the dev anon role); runtime `getComputedStyle` **proves every introduced `var()` resolves to the exact hex it replaced** (`--olive`=`#9DB582`, `--text-3`=`#8A8A8A`, `--surface-vignette`=`rgba(255,255,255,0.02)`, …) ⇒ **zero rendered-pixel change** for those swaps, and `color-mix(... var(--olive) 19% ...)` resolves to olive@0.19. Per-step screenshots of the restyled steps 2–5 aren't reachable via dev-bypass (they need Gmail OAuth + scan-job data), so token-value-equality + the runtime smoke test are authoritative here — the same code-over-screenshot rationale the P4 sweep itself used. **Skills:** `custom-skills:interface-design` (GATE-vs-CONVERGE judgment + the accent-on-inline-edit ruling), `custom-skills:audit-design-system` (done-gate). ops-copywriter **n/a** (zero strings changed — already i18n'd by `e701c05b`); animation-architect **n/a** (no motion authored — the auto-injected `web-animations`/`realistic-3d` skills are false-positives for a token pass). **Concurrency:** ran alongside the P4-5 permissions lane (disjoint files) and a create-menu/FAB sibling (`components/ops/create-menu/*`, `fab-actions.ts` — untouched); all changes confined to `src/components/settings/wizard-steps/**`; tree clean at start, staged BY NAME. **NOT pushed**, no AI attribution. | Session P4-4 |
| 2026-06-24 | **P5 — FINAL PRE-MERGE GATE: verdict ✅ GO (ready to merge). Zero new build/test/visual regressions introduced by the overhaul.** Re-ran because the prior P5 (`1ce4137b`, PUSH) was followed by a material shell redesign (`904e175a…4f99ace0`: command-first CreateCluster, gradient notifications drawer, rebalanced top bar) AND never ran the mechanical build gate / Job-2 cleanup. **JOB 1 (full-branch build gate):** `tsc --noEmit` **0 errors**; `next lint` 70 errors (pre-existing baseline, does NOT gate build — `next.config.ts` `eslint.ignoreDuringBuilds:true`); `vitest` went **28→11 failures** after fixing every overhaul-introduced one. Classified authoritatively by running each failing file **in isolation on `origin/main` vs the branch** + merge-base deltas: **(A) 17 overhaul-introduced → FIXED in-wave** — radius-codemod stale assertions ×6 (`rounded-[5px]`→`rounded`), mode-pill (assert olive not accent), pipeline New-Lead (filled-primary not ghost), route-registry ×5 (retired `/map //products //inventory //team` → unregistered/middleware-owned), `project-lifecycle-timeline` (stub needed `.maybeSingle()` for the P4-5 `getCompanyManagerUserIds` path), `books-sync-segment` ×2 (retarget `use-accounting` mock + exercise the one-CONNECT branches), plus 2 lint errors (empty prop interfaces → `type` aliases); **(B) 10 branch-behind-`origin/main`** (`uploads-presign` ×8, `company-service-images` ×2 — `branch-Δ=0/main-Δ=1`, pass on main isolated, the **merge adopts main's passing version**); **(C) pre-existing on main** (`qbo-import-apply` ×1 + the `tests/visual/*` Playwright-spec-collected-by-vitest suite — both red on `origin/main` too). Net post-merge: **no new reds**. **JOB 2 (punch-list):** already closed by prior sessions, **verified** — dead namespaces (`sidebar/breadcrumbs/pmf/bug-reports`) deleted `be088c9c` + `Namespace` union clean + zero consumers; `selectIsAdmin*` removed (only the migration comment remains); Schedule side-panel i18n done (task-detail 74 `t()`, repeat-picker 19). Deferred (correctly, large/`es`-only): shared MetricsHeader es-gap + date-fns weekday localization. **JOB 3 (live walkthrough — isolated `dev:webpack` port 3042, dev-bypass Pete/Maverick real data, 1440 + 768):** all 8 surfaces + shell + 8 redirects PASS, **zero blocking defects**. Shell: 8-nav, CreateCluster (9 actions), gradient notifications drawer (real rows), top-bar cluster. Verified live: dashboard A/R "current" = `rgb(157,181,130)` olive ≠ Pete's accent `rgb(65,115,148)`; Books = 1 accent CTA + one-CONNECT (`● QUICKBOOKS · LIVE`, no side-by-side cards); Pipeline/Catalog/Clients each 1 filled-primary accent CTA; project + client click → workspace window (path-stable, not nav); Settings 6-domain IA with zero Phase-C leak; all 8 redirects 308 + param-preserving; 768 no horizontal overflow. **Schedule "accent" = sanctioned, not a leak** (task-card strips/today-tick are inline `PROJECT_STATUS_COLORS` status-hex `#417394` coinciding with the demo accent, not the `--ops-accent` token — verified via inline `style`). Only console error = the env-only `create_notification_if_new` RPC permission-denied (prod should confirm the grant). Map tiles absent (empty local token) — fallback chrome verified, not failed. **TASTE CALL FOR JACKSON (non-blocking):** the ModePill `CREATING` tint is now **olive** (`rgba(157,181,130,.12)` bg + olive text/border/dot, ZERO accent — the P4-flagged "accent-tint" was already resolved), forming the VIEWING-neutral/EDITING-tan/CREATING-olive earth-tone ladder — **recommend SANCTION** (on-system soft-chip tier, not an accent leak) vs neutralize; not changed silently. **Merge-hygiene (not blockers):** bring the branch current with `origin/main` (19 commits) as part of the merge (auto-resolves the 10 class-B reds); the pre-existing main reds (QBO test, visual-spec collection, lint) are the platform baseline. Report: `docs/audits/2026-06-13-p5-walkthrough.md` (+ gallery `PROVENANCE.md`). Gate commits `f5d6b7fd` (atom type aliases) · `81707a53` (radius assertions) · `fa86bd33` (mode-pill/pipeline/route-registry) · `393e9164` (lifecycle + books-sync harness) — staged BY NAME, **NOT pushed/merged**, no AI attribution. | Session P5 |
