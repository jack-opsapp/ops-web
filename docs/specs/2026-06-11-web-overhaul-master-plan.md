# OPS-Web Overhaul — Master Plan

**Status:** Active — P1 (decisions) complete · P2 (shell) shipped · P3.1 (Books) shipped
**Decision authority:** Jackson, 2026-06-11
**Document role:** Program-level spec for the full production-polish overhaul of OPS-Web. Every phase session works from this document. Each phase runs its own spec → plan → build → verify cycle; this document defines the destination, the verdicts, and the rules. It does not duplicate per-surface design detail — that is produced inside each phase.

---

## 1. Mission

OPS-Web must look and feel like a product people confidently pay for: uniform, consistent, polished, production-ready on every surface. The bar is the design system's own reference points — xAI, SpaceX mission control, Apple Pro apps. Anything that reads as a loose interpretation, a tech demo, or an old aesthetic fails.

**Definition of done:** every surface in the final IA conforms to `ops-design-system/project/DESIGN.md`, passes the P4 cross-cutting checklist, and survives the P5 full walkthrough with a punch list at zero.

---

## 2. Final information architecture — 8-item nav

The sidebar goes from 16 items to 8. No dividers-as-apology: the list is short enough to need no grouping debate.

| # | Label | Route | Absorbs | Redirects (must preserve query params) |
|---|-------|-------|---------|----------------------------------------|
| 1 | DASHBOARD | `/dashboard` | — | — |
| 2 | PROJECTS | `/projects` | Map (becomes third view mode: canvas / table / map) | `/map` → `/projects?view=map` |
| 3 | SCHEDULE | `/schedule` | — (rename of Calendar) | `/calendar` → `/schedule` |
| 4 | PIPELINE | `/pipeline` | — | — |
| 5 | BOOKS | `/books` | Estimates, Invoices, Accounting (A/R), Expenses (currently orphaned components, no route), Cashflow placeholder | `/estimates` → `/books?segment=estimates` · `/invoices` → `/books?segment=invoices` · `/accounting` → `/books?segment=invoices&view=aging` (exact mapping decided in-phase) · `/money/cashflow` → `/books` |
| 6 | CATALOG | `/catalog` | Products, Inventory (segments: PRODUCTS / STOCK, matching iOS Catalog tab) | `/products` → `/catalog?segment=products` · `/inventory` → `/catalog?segment=stock` · `/products/[id]/options` → equivalent catalog path |
| 7 | CLIENTS | `/clients` | — (rebuilt from scratch) | — |
| 8 | SETTINGS | `/settings` | Team (members, invites, roles, permissions become a Settings section) | `/team` → `/settings?section=team` (exact param decided in-phase) |

**Removed from the default nav** (invisible to all non-flagged companies):

- **Inbox** — shelved (see §3). Route stays flag-gated for Canpro.
- **Calibration** — Phase C surface; flag-gated, reachable for Canpro only.
- **Agent Queue** — Phase C surface; appears in the nav (bottom group, near Settings) **only** for companies with the Phase C flag. Everyone else never sees it.
- **Testing Grounds** — dev-only; must not ship in the production build/nav.
- **Cash Forecast placeholder** (`/money/cashflow`) — absorbed by Books.

**Redirect rule:** every retired route redirects to its absorbing surface. Notification `action_url`s stored in the database (e.g. `/invoices`, `/calendar`) must keep resolving. P4 verifies this end-to-end.

**Schedule rename scope:** route, sidebar label, top-bar title, en + es dictionaries, FAB actions, and all internal links say "Schedule." The current state — sidebar "Calendar," top bar "Schedule" — is exactly the class of drift this overhaul exists to kill.

---

## 3. Inbox / Phase C posture (decided 2026-06-11)

- **The in-app Inbox UI is shelved.** It does not appear in the nav and gets no polish work until the initiative is explicitly resumed. Do not route new features through an in-app inbox surface.
- **The email engine continues headless.** It pipes data (drafts, extracted leads, learning) into and out of the user's connected Gmail / Outlook mailbox. The user's real inbox is the inbox.
- **Phase C (agent functionality) is Canpro-only** until it is 100% production ready. The app must work fully without Phase C; Phase C is a supercharger layered on top, never a dependency.
- **Calibration and Agent Queue** are Phase C operator surfaces: gated behind the existing per-company feature flag, visible/reachable only for flagged companies (today: Canpro). The P2 session must read the current flag mechanics (per-company `inbox_ui` flag, middleware redirects, `feature-flag-definitions.ts`) and extend them to cover nav visibility — verify in code, do not assume this document's naming is exact.

---

## 4. Surface verdicts

| Surface | Verdict | Notes |
|---------|---------|-------|
| Shell (sidebar, top bar, avatar menu, drawers, FAB) | **Rebuild from scratch** | Phase 2. See §5 |
| Books | **Build new** | Replaces three old single-file pages + orphaned expense components |
| Catalog | **Build new** | Inventory page is a 116-line stub; Products is mid-size and absorbed |
| Clients | **Rebuild from scratch** | Adopt the floating-window interaction model (see below) |
| Dashboard | Keep — light polish pass | User-approved baseline |
| Settings | Keep — renovate + absorb Team | Gains team/members/roles section |
| Pipeline | Keep — conformance audit only | Recent heavy build |
| Projects | Keep — absorb Map as view mode | Recent heavy build |
| Schedule (Calendar) | Keep — rename + conformance audit | Recent heavy build |
| Estimates / Invoices / Accounting / Team / Map / Cash-forecast placeholder | **Absorb & retire** | Old pages deleted once parity confirmed |
| Inbox / Calibration / Agent Queue | **Gate, don't touch** | §3 posture |
| Testing Grounds | **Remove from production** | Dev tooling only |

**Feature-parity rule (non-negotiable):** any session absorbing or rebuilding a surface must read the old surface's code top-to-bottom, produce a capability inventory, and reach 100% parity in the replacement — or list descoped items explicitly and get Jackson's sign-off. No silent feature loss. "The old page had it" is a launch blocker, not a footnote.

**Interaction model:** the floating workspace window is canonical for entity detail (already true for Projects — `/projects/[id]` survives only as a thin SEO/deep-link fallback). The Clients rebuild adopts the same model: click a client → client workspace window; `/clients/[id]` becomes a thin fallback. Any control anywhere in the app that still *navigates* to a dedicated detail page instead of opening the window is a bug (P4 sweeps for these).

---

## 5. Phase 2 — shell rebuild scope

Everything else lives inside the shell, so it goes first. One spawned session: **`WEB OVERHAUL - P2-1`**.

In scope, as one cohesive design (mockups before code):

- **Sidebar** — 8-item nav per §2; revisit the hover-expand rail behavior (currently collapsed-at-rest 72px → 256px overlay on hover) as an explicit design decision; mobile drawer; company header; user section.
- **Avatar menu** — full redesign. The current generic dropdown (Settings / OPS Website / Courses / Download iOS App / Sign Out) is off-brand; note the "Download iOS App" item currently links to `"#"` — dead link, fix or cut.
- **Top bar** — page titles must come from a single i18n-compliant route registry shared with the sidebar (today `top-bar.tsx` hardcodes English titles in a local map — this is the Calendar/Schedule drift's root cause); breadcrumbs, search affordance, sync indicator, undo button all re-examined as one composition.
- **Notifications drawer + quick-actions edge tabs** — redesign for cohesion with the new shell; the user has flagged the drawer as clunky.
- **FAB** — audit every action against the new IA (`fab-actions.ts` route handlers point at `/expenses`, `/inventory`, etc. — retarget to absorbing surfaces).
- **Sidebar data hooks** — inbox unread badge is removed with the Inbox nav item; agent-queue badge renders only under the Phase C flag.

Constraints: DESIGN.md §9 components (no accent on nav, 2px text-2 active bar, 6px sidebar radius), z-index scale from `OPS-Web/CLAUDE.md`, work in a git worktree (never branch-switch the primary checkout).

---

## 6. Phase plan

| Phase | Content | Session(s) |
|-------|---------|-----------|
| **P1** | Decision record — this document | done (2026-06-11) |
| **P2** | Shell rebuild (§5) | `WEB OVERHAUL - P2-1` |
| **P3** | Surface waves, priority order: **3.1 Books** (kills 4 nav items) → **3.2 Catalog** → **3.3 Clients** → **3.4 Settings + Team absorption** → **3.5 Projects map-view absorption** → **3.6 Schedule rename** → **3.7 Dashboard + Pipeline + Schedule conformance/polish passes** | `WEB OVERHAUL - P3-1` … `P3-n` |
| **P4** | Cross-cutting consistency sweep (§7) | `WEB OVERHAUL - P4-1` |
| **P5** | Final walkthrough: run the app, every surface, multiple viewports, screenshot gallery, punch list to zero | `WEB OVERHAUL - P5-1` |

**Sequencing rules:** P2 lands before any P3 wave starts (pages land inside the corrected frame). P4 starts only when all P3 waves have landed. Spawn titles follow the convention exactly; ordinals continue across sessions (next spawn in a phase = highest existing ordinal + 1).

**Branch & publication rule (Jackson, 2026-06-11):** the entire overhaul builds on the **single local feature branch `feat/web-overhaul`**, checked out in the `ops-web-overhaul-p2-shell` worktree (directory name is historical — don't rename it, the branch inside is what matters). No per-task, per-phase, or per-surface branches. **Nothing is pushed to GitHub until Jackson explicitly authorizes publication.** Consequence: overhaul waves land **sequentially** on the branch — one overhaul session works it at a time; confirm no sibling overhaul session is mid-wave before starting. Commits stay atomic per the standard rules; this changes where work lands, not how it's committed.

**Transition rule (no broken nav, ever):** P2 ships the rebuilt shell on a **data-driven nav registry** designed for the end-state 8-item IA, but populated only with routes that exist at ship time — Estimates / Invoices / Accounting / Products / Inventory entries remain (inside the new shell design) until their absorbing surface lands. The Schedule rename may ride P2, since `/calendar` already exists. Each P3 wave's landing commit updates the registry — swapping the absorbed entries for the new surface and adding the §2 redirects in the same commit. At no point does the nav link to a route that 404s, and at no point does a surface lose its nav entry before its replacement is live.

**Per-session protocol (every phase session, mandatory):**

1. Read this document, `ops-design-system/project/SKILL.md`, `README.md`, and `DESIGN.md` (+ `colors_and_type.css` tokens), and relevant `ops-software-bible` sections.
2. Read the old surface's code top-to-bottom for the parity inventory (§4).
3. Run its own brainstorm/spec → plan → build cycle; mockups/wireframes before code for any new layout.
4. Verify in the running app with screenshots before claiming done.
5. Commit atomically (stage by name, no AI attribution), respect parallel-session WIP, update the relevant bible section in the same session.

**Design enforcement (added 2026-06-11 after wave output was judged a "cheap replica" of the system):**

- `ops-design` is **not an invocable skill** — it is a directory of files. Reading it is mandatory but reading alone has demonstrably NOT produced on-brand output. The following are hard requirements, not suggestions.
- **Invoke these registered skills via the Skill tool, by exact name, at the stated moment:** `frontend-design:frontend-design` or `custom-skills:interface-design` before building any page or component; `ops-copywriter:ops-copywriter` before writing any user-facing string; `animation-studio:animation-architect` before any motion/transition work; `custom-skills:widget-builder` for anything widget-shaped. If a skill fails to load, say so in the session log rather than silently proceeding.
- **Composition is traceable or it doesn't ship:** every new panel, table, toolbar, header, and strip must trace to a reference component in `ops-design-system/project/ui_kits/ops-web/` or to DESIGN.md §9 (components) / §10 (widget anatomy). Token-compliant colors on an off-system composition still fails. If no reference exists for what you need, mock it and get Jackson's approval before writing code.
- **Hard done-gate:** before reporting a wave complete, invoke `custom-skills:audit-design-system` against every file created or modified in the wave, fix every high-severity finding, and include the audit summary in the landing report. A wave without its audit attached is not done.

**UX-judgment gate (the overarching principle — Jackson, 2026-06-12):**

Critical thinking about layout, organization, and information presentation **is the work itself**, not a review step. Every presentation decision must be *reasoned from the human's situation* — what they're trying to accomplish right now, how often they do it, what state they're in, what they should see first — never merely *rendered* from the data model, the feature list, or this spec's letter. At the mockup stage, before Jackson sees anything, run an explicit information-architecture critique: for **every element on the screen**, answer *"why is this the right presentation, for this user, at this moment?"* Any element whose only justification is "the option/data/feature exists" gets redesigned, not shipped. Jackson reviews taste; coherence must already be guaranteed — he must never have to catch a design-thinking failure retroactively.

Canonical failure: Books rendered side-by-side QuickBooks AND Sage connection cards because two providers exist in the data model — but a user picks one provider, once, ever. Correct: a single CONNECT ACCOUNTING SOFTWARE entry point → brief provider flow → compact live badge once connected → settings/disconnect/switch in a modal behind the badge. Derived applications (illustrative, NOT exhaustive — the principle is the requirement): either/or choices collapse to one entry point; prominence proportional to frequency of use (once-ever setup never owns permanent prime space); state-aware layouts that show the user's current reality, not all possible realities; the schema is not the interface; verbs out of scan surfaces; progressive disclosure over page acreage; ruthless omission — every element earns its place.

---

## 7. P4 cross-cutting sweep checklist

Run as systematic greps + a full visual walkthrough. Every item is pass/fail per surface:

- [ ] Every project click opens the workspace window — zero navigations to `/projects/[id]` from interactive controls (thin fallback route excepted)
- [ ] Every client click opens the client window (post P3.3)
- [ ] Copy voice (ops-copywriter pass): UPPERCASE authority, `//` panel prefixes, `[brackets]` micro-text, no exclamation points, no emoji, no "Welcome back!"-class copy
- [ ] Numbers: always JetBrains Mono, tabular-lining, slashed zero, formatted (`$12,480`, `87%`), `—` for empty — never `N/A`, never raw floats
- [ ] Empty / loading / error states present and on-spec for every surface
- [ ] i18n: zero hardcoded user-facing strings (top-bar route titles are a known offender); en + es dictionaries complete
- [ ] Icons: `lucide-react` only (no `@carbon/icons-react` imports), sizes 16/20/24/32, `currentColor`, monochrome
- [ ] Accent `#6F94B0` only on primary CTAs + focus rings — never nav, links, toggles, tags, icons
- [ ] Radius / spacing / color trace to tokens (run `custom-skills:audit-design-system`)
- [ ] Motion: single easing `cubic-bezier(0.22, 1, 0.36, 1)`, no spring/bounce, `prefers-reduced-motion` fallbacks everywhere
- [ ] Z-index: touched components migrated to the new scale
- [ ] All §2 redirects work; old notification `action_url`s resolve; no orphaned routes or dead dictionary keys
- [ ] Permissions: granular `has_permission` checks only — zero role-name filtering
- [ ] No box-shadows on dark surfaces; glass tiers used correctly; no triple-stacked glass

---

## 8. Inherited non-negotiables

`DESIGN.md` is law for every visual decision. Root `CLAUDE.md` + `OPS-Web/CLAUDE.md` rules apply in full (perfection standard, no deferral, commit discipline, bible updates). Where this document and the design system conflict, the design system wins and this document gets corrected.

---

## 9. Decision log

| Date | Decision | By |
|------|----------|----|
| 2026-06-11 | 8-item nav approved (Dashboard, Projects, Schedule, Pipeline, Books, Catalog, Clients, Settings) | Jackson |
| 2026-06-11 | Calendar renamed Schedule (route + labels) | Jackson |
| 2026-06-11 | Rebuild from scratch: shell, Books, Catalog, Clients | Jackson |
| 2026-06-11 | Inbox UI shelved; email engine runs headless through the user's connected Gmail/Outlook | Jackson |
| 2026-06-11 | Phase C (agent, Calibration, Agent Queue) Canpro-only until 100% production ready; app never depends on it | Jackson |
| 2026-06-11 | Map absorbed into Projects as view mode; Team absorbed into Settings | Jackson |
| 2026-06-11 | P2 sidebar: hover-to-expand rejected on live review (jarring); fixed 72px instrument rail with right-flyout tooltip labels approved and shipped (cc4f78a6, shell spec §9 revision) | Jackson |
| 2026-06-11 | Single local branch `feat/web-overhaul` for the entire program; no per-task branches; no GitHub pushes until explicitly authorized; waves land sequentially | Jackson |
| 2026-06-11 | **P3.1 Books direction A ("Instrument Strip") approved** — Jackson delegated the pick ("whatever you think is the best UX") across three live-reviewed mockups; A chosen for iOS-pattern fidelity, table width, and clean permission degradation. Mockups + capability inventory + plan: `docs/{design/2026-06-11-books-mockups, specs/2026-06-11-books-capability-inventory.md, plans/2026-06-11-books-surface.md}` | Jackson (delegated) |
| 2026-06-11 | **P3.1 Books shipped** — `/books` with ledger strip (NET/CASH FLOW/A-R/JOBS) + 4 segments (INVOICES · ESTIMATES · EXPENSES · SYNC); zero descopes against the capability inventory. §2 row-5 mapping confirmed: `/accounting` → `/books?segment=invoices&view=aging` (tab-aware: expenses→expenses, integrations→sync, import→sync+import); `/books/cashflow` (iOS forecast_dip action_url) added to the redirect map. Registry gained `anyOfPermissions` (route gate = invoices.view ∨ estimates.view ∨ expenses.approve ∨ accounting.view; `.approve` because the web expenses segment is the review hub); `/books` joined the `accounting` feature-flag route list. Old pages deleted after live verification (gallery in `docs/design/2026-06-11-books-mockups/verification/`). Known pre-existing red: `tests/unit/inbox/ai-draft-provenance.test.ts` (lifecycle-learning flag drift, unrelated to this wave) | Session P3.1 |
| 2026-06-11 | Market-intel research (`WEB OVERHAUL - P3-1-1`) folded into Books: A/R-at-a-glance + per-job profit are the trades-loved surfaces; nothing auto-sends in the operator's voice; plain-language labels over accountant-speak | Session P3.1 |
| 2026-06-11 | **P3.1 adversarial review remediated** — 28/33 audit findings confirmed and fixed in-wave (local-date period boundaries, date-based `status=overdue` A/R drill, hydration-safe segment default, full 11px/token/motion/i18n conformance, widget `/new` dead links). Two items pending Jackson sign-off in inventory §8: MetricsHeader mini-viz presentation drop (data parity is complete) and the pre-existing `accounting.view`-only empty-data persona | Session P3.1 |
| 2026-06-12 | **P3.2 Catalog direction D ("Workbench") approved + shipped** — Jackson rejected pure-efficiency framing ("think about the flow, what they're likely looking at, how they interact") on the first A/B/C mockups; a 5-agent research workflow (trades-owner forums + reviews, inventory-UX patterns, iOS parity, data feasibility) + synthesis produced **direction D**, organized around three ranked flows — buy-run triage, count/receive, price-book upkeep — then hardened by a 3-lens adversarial critique (owner realism / design-system / overpromise-vs-live-data). Jackson approved D for the build. `/catalog` ships with PRODUCTS / STOCK segments (mirrors iOS Catalog tab): a 3-tile **supply strip** (STOCK HEALTH / ON-HAND / PRODUCTS) where every tile is an owner question with a working pivot and honest zero states (uncosted tenants see `—`, never fake zeroes; UNTRACKED is its own health bucket — threshold-less stock is never "OK"); STOCK = flat variant-aware rows (family + option-value label) with inline QTY editing (set or signed delta, **audited** to `inventory_deductions` keyed by `catalog_variant_id`, advances down the column), buy-run pivot with COPY LIST/PRINT + coverage-honest BUY-TO-THRESHOLD, opt-in GROUP::FAMILY, a right detail drawer (quick-adjust + unit-cost + 3-level threshold-cascade sources + used-in reverse links + web/iOS adjustment ledger), bulk delete, and a kebab (MANAGE categories/tags/units/threshold-defaults · saved counts · CSV import); PRODUCTS = tri-coupled COST/PRICE/MARGIN inline cells + NO-COST worklist + CONFIG counts + quick-add. New full editor at `/catalog/products/[id]` (base fields + options + modifiers + recipe — the estimate-builder read path survives intact) **fixes the iOS "VIEW ON WEB →" deep link to `/products/{id}` that 404s today** (`ProductDetailView.swift:1054`). Data layer reads `catalog_*` directly (RLS `company_isolation` on `{public}` verified) — the variant-blind `inventory_*` compat views are abandoned, not extended; snapshots write `catalog_snapshots` directly. Registry swap: Products+Inventory → CATALOG (order 9, `Library` icon, anyOf `products.view`/`inventory.view`); middleware 308s `/products`→`?segment=products`, `/inventory`→`?segment=stock` (action=new preserved), `/products/{id}`+`/products/{id}/options`→`/catalog/products/{id}` — **all verified live**. Dead `products`/`inventory` feature-flag config removed (never real `feature_flags` rows; only pipeline+accounting exist). Capability inventory + §6 scope-additions: `docs/specs/2026-06-11-catalog-capability-inventory.md`; mockups + rationale: `docs/design/2026-06-11-catalog-mockups/`. Live-verified on port 3017 with real Maverick data (67 SKUs / 20 products / 3 costed → $500 on-hand / 7 missing cost / 45% avg margin); both segments + all 3 redirects confirmed (route 200, tsc 0 catalog errors, eslint clean). Note: shared preview browser contended with an active sibling Books session (HMR churn) — stock + products segments captured between collisions. Commits `f47cb2ba`…(retirement) | Jackson (D approved) / Session P3.2 |
| 2026-06-12 | **P3-3 design-conformance remediation landed** — the 2026-06-11 audit punch list (20 high / 42 med / 14 low) driven to zero across shell + Books, except the control-size findings VOIDED by Jackson's 2026-06-11 sizing ruling. Systemic: slashed-zero + tabular numerals enabled globally (`"tnum" 1, "zero" 1` on every mono site); DESIGN.md §9 amended with the ratified **compact workbar tier** (28px controls / 22–24px chips for data-dense workbars; 36px = standard button not a floor; §15 44px row marked iOS-only) — NOTE: `ops-design-system/` is not a git repo, so the amendment is a file edit with no commit. Shell: accent stripped from edge-rail chrome (EdgeTab "accent" member deleted; attn states glaze tan), top-bar blur-12 chip glass → flat interactive surfaces, sidebar → glass-surface tier + top gradient, all 0.04/0.06 hairlines → --line/--glass-border, 22 sub-11px sites → 11px floor, Cake Mono confined to 14/11/22 roles, breadcrumbs → kit mono-11 `//` pattern with Cake leaf titles, drawer headers → kit `// TITLE` widget anatomy, drawer paddings centralized (EDGE_DRAWER_PADDING), durations → 150/200ms tokens + keyframes on the OPS curve + full reduced-motion coverage. Books: per-row icon toolbars → one labelled ACTIONS overflow (zero capability loss), coach-mark empty states → tactical `0 INVOICES` form, es/notifications + es/quick-actions fully translated ("__" TODO markers deleted), payment terms + sync statuses localized, `Intl.NumberFormat("en-US")` ×3 → active locale, shared Button → Cake Mono 300 14px. Deviations from audit fix-text, by judgment: tooltip chips at 4px radius (12px on a 26px chip = pill, §14 ban), ledger strip keeps 16px internal gap as one composite instrument (documented exception), rail badges normalized to surface-active fill. Done-gate: `custom-skills:audit-design-system` re-run over all 27 touched files — **zero high findings**; 3 gate residuals fixed in `8a154b74`; pre-existing out-of-scope items flagged for P4 (`.ops-badge` rounded-sm legacy utility, unused `.ops-card-accent` anti-pattern utility, Calibration/pipeline legacy easings, table-v2's own Cake-12 workbar labels now non-compliant with the amended §9). `npx tsc --noEmit` clean; before/after verified live on port 3017 (slashed zeros, flat top-bar chips at 0.04/0.10/5px, semantic-only rail stripes confirmed via computed styles). Commits `8aee46af`…`8a154b74` (9 atomic, by theme) | Session P3-3 |
| 2026-06-12 | **P3-5 Books table + toolbar refinement landed** — the three punch-list items (`docs/audits/2026-06-12-books-refinement-punchlist.md` §2/§3/§4) to done. **§2 architecture decision (delegated "decide and document"):** the projects/pipeline table-v2 are full TanStack data grids (saved views, virtualization, inline cell-edit, zoom/density, domain-typed column registries, ~26 files each); copy-adapting a third time imports machinery read-mostly registers never exercise, and extracting the framework wholesale would destabilize two shipped surfaces. Chose a **scoped presentational extraction**: new shared `src/components/ui/register-table/` (`RegisterTable` + cell atoms `TableNumber`/`TablePrimary`/`TableMeta`/`TableMono`) reproducing the table-v2 row anatomy (mono-13 number, Mohave-14 name, mono metadata, earth-tone `Tag` status, one labelled ACTIONS overflow) without the grid framework; Books invoices + estimates adopt it, replacing the two hand-rolled `<table>`s. Projects/Pipeline untouched (zero risk); converging them onto the shared atoms is **deferred debt**. **Naming:** a pre-existing **unused** `ui/data-table.tsx` (generic admin grid: column-visibility toolbar, pagination, no row-click/actions — wrong fit) collided with the intended name and is dead code (zero consumers) → new module named `register-table` to avoid touching a file this wave didn't create; the dead `data-table.tsx` is flagged for separate cleanup. **Scope corrections (punchlist imprecision, verified in code):** the expenses segment mounts `ExpenseReviewDashboard` (a master-detail review queue, no `<table>`) and the A/R aging view has no `<table>` (aging bars + top-clients list + breakdown cards) — neither converted; converting either would be a regression. **§3** — list/aging promoted from a filter-row `FilterChips` pair to a shared `SegmentControl` in the stable `workbar`, pinned far-right (Jackson-approved placement), present + identically positioned in both states (verified live: toggle container right edge/top **identical** list↔aging at 1290/286); search is list-only and sits left of the toggle so it never shifts; state-aware (rendered only when the user can reach both views); `ArAgingView.onBackToList` divergent control removed (workbar toggle is the single way back). **§4** — inline New Invoice / New Estimate buttons removed from workbars + empty states (FAB owns creation, `fab-actions.ts`, same perms); empty states state the fact only (`0 INVOICES` / `0 ESTIMATES`), no coach-mark/button per DESIGN.md §2; also drops the last accent from these data-dense workbars. **Process:** mockup approved pre-code (toolbar + row); UX-judgment gate applied to the whole toolbar (every element justified). **Done-gate:** `custom-skills:audit-design-system` over all touched files — **zero high findings** (no bare hex; no raw rgba — row hairline → `border-border-subtle` token; arbitrary-px + `rounded-[4px]` match the shipped kit/compact-tier conventions and the shared `Tag`/`FilterChips`; the systemic `rounded-[Npx]`→named-token question is pre-existing codebase-wide, noted for P4). **No dictionary strings changed** (all keys reused: `view.list`/`view.aging`/`segment.*`/`*.empty.*`/`*.table.*`) → ops-copywriter n/a. `npx tsc --noEmit` + `eslint` clean; verified live on 3017 (invoices list full-width, aging view, estimates), no console errors. Commits `fc15c306` (primitive) + `f54ca6da` (adoption + toolbar) | Session P3-5 |
| 2026-06-13 | **P3.3 Clients rebuilt (Direction B) + retired** — Jackson picked **Direction B** (lean list + tabbed window) from two inline-rendered mockups, then the table-foundation decision (recon surfaced that "use table-v2" conflicts with B's lean design): **`RegisterTable`** — the P3-5 presentational extraction of table-v2 — so "adapt the table-v2 definition" is satisfied without forking the heavy grid or adding DB work. Both descopes approved: **D1** one canonical table (no card/table toggle; sub-contact peek moves into the window), **D2** create folds into the window's `creating` mode. **List** (`/clients`): a slim rose **A/R banner** ("N clients owe $X — oldest Nd", chase→OWES filter) + search + one accent `+ NEW CLIENT` + ALL/WITH PROJECTS/OWES/NEW chips + a glass `RegisterTable` (client/contact/projects/outstanding/last-seen); the scope-aware `clients.view` gate is preserved verbatim; per-client outstanding + recency are real client-side aggregates over the shared invoice/project caches — **no new DB** (join on `invoices.client_id`, NOT the 100%-NULL `client_ref`; exclude paid/void/draft/written_off; "oldest" = oldest **overdue**). **Window**: a `ClientWorkspaceContainer` reusing the generic `ProjectWorkspaceWindow` shell directly + a cloned `ClientViewingTabs` (own layoutId); viewing/editing/creating modes; tabbed dossier **CONTACT** (contact actions + inline sub-contact CRUD + notes) / **PROJECTS** (active+done, row→project window) / **MONEY** (invoiced/paid/outstanding/overdue + invoice list + paid-bar, gated `invoices.view`) / **ACTIVITY** (real composed timeline: projects + invoices sent/paid/past-due + won opps). New cache-shared hooks `useClientOutstandingMap`/`useClientFinancials`/`useClientActivity` (no query-key churn). **Wiring**: `client-workspace` window type + `openClientWindow` + `?openClient=` deep-link (`=new`→creating) in window-store + dashboard-layout; FAB + widget entity-open retargeted; `/clients/[id]` + `/clients/new` → param-preserving redirects (command-palette/pipeline/inbox route-opens land on the window via the redirect, mirroring projects). **Retired**: client-detail popover + store, legacy create/edit client modals, the `create-client` window type (all callers retargeted). **R8 reconsidered (design judgment):** client CRUD stays toast-only — self-action rail notifications are noise. Full UI/UX skill stack invoked (animation-architect/frontend-design/interface-design/ops-copywriter/wireframe/ui-ux-pro-max/data-visualization) + `audit-design-system` done-gate (accent on CTA + focus-rings only; no bare hex; 11px floor; mono tabular) + a **4-lens adversarial review workflow** (correctness/permissions/parity/design) — no critical/high bugs; review-driven hardening landed (clients.edit/create gates on the form + EDIT button, not-found state, restored create funnel telemetry, A/R "oldest-overdue" + past-due activity correctness, money-tile scale). P4 flags: shared `RegisterTable` keyboard focus-ring (Books-owned → spawned `WEB OVERHAUL - P3-3-1`); command-palette/pipeline/inbox direct-open retarget (works via redirect today). `tsc` 0 / eslint clean; live-verified on 3017 with real Maverick data (67 clients / $38,174.88 A/R) — list, row→window, all 4 tabs, edit, create-via-redirect, [id]+new redirects, deep-link (shared preview contended with the sibling Books session + a slow gmail-review-items API; verified between collisions + server-side 200s). Spec `docs/specs/2026-06-13-clients-rebuild-implementation.md`; mockups `docs/design/2026-06-12-clients-mockups/`; inventory `docs/specs/2026-06-12-clients-capability-inventory.md`. Commits `813a24d8`…HEAD (9 atomic, by theme) | Jackson (B + descopes) / Session P3.3 |
| 2026-06-13 | **P4-1 cross-surface visual cohesion audit landed (report only, no source changes)** — live 12-shot visual pass on 3017 + 3 read-only code agents + first-hand primitive source reads, against DESIGN.md/kit. Verdict: shell + Books + Clients read as one product (strong baseline); **Catalog is the lone outlier — it re-derived the kit** (forked `CatalogSegmentControl` at off-scale Cake-13, hand-rolled `<table>`s + per-row icon actions, forked Tag/FilterChips/DrillChip, Button-override CTAs, raw `<select>`/rgba) = the cheap-replica failure localized to one surface. **Token/primitive pass WARRANTED (yes):** (1) extract one `InstrumentStrip` primitive — Books `// LEDGER` vs Catalog `// SUPPLY` are twins with 8 drifts incl. an off-curve rAF count-up in Catalog; (2) **retire `EmptyState`** — it's itself the banned coach-mark (icon+title+desc+CTA, DESIGN.md §2) so registers correctly avoid it but hand-roll inconsistently → mint a tactical register-empty; (3) unify 3 form-error color dialects (Input #B58289 hardcode / Textarea ops-error / others rose token); (4) delete dead `.glass-subtle` tier; (5) codify A/R-aging current color — Books=olive (on-palette), Dashboard widget=`#417394` off-palette (no blue financial token; accent does NOT leak — verified). Cross-surface decision needed (Jackson): create affordance diverges (Books FAB-only vs Clients `+ NEW CLIENT` vs Catalog `+ ADD`). Class-B (Dashboard/Pipeline/Schedule/Projects) catalogued as upcoming-wave scope, not defects (universal `tracking-wider`→`tracking-[0.16em]`; Schedule's 3 legacy easings; RegisterTable convergence is documented deferred debt). Report + gallery: `docs/audits/2026-06-13-cross-surface-visual-cohesion.md` (+ `2026-06-13-visual-cohesion/gallery/`, 12 shots). Folded known residuals (data-table.tsx confirmed removed `55795c06`; .ops-badge/.ops-card-accent; table-v2 Cake-12; focus-ring `P3-3-1`; systemic `rounded-[Npx]`). | Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.1/B1 — `InstrumentStrip` primitive extracted; Books + Catalog strips converged** — the two hand-rolled instrument strips collapsed onto one primitive at `src/components/ui/instrument-strip/` (`InstrumentStrip` section+header / `GlanceGrid` / `GlanceTile` / `TileHero` (optional `tone`) / `TileSub` / `ScopeBadge` / `GlanceTileSkeleton` + the single shared `useCountUp`). Promoted verbatim from the **canonical** Books `ledger-strip` (approved pixels `direction-a-instrument-strip.html`); Books now consumes it **pixel-identically** (every extracted className byte-for-byte the originals — `GlanceTile`=`TileShell`, `TileHero` default branch=`text-data-lg leading-tight text-text tabular-nums`, `GlanceGrid` emits the same classes order-independent; render maps 1:1; per-surface mini-viz `MarginMeter`/`WeeklySparkline`/`AgingRamp`/`DivergingBars` stay slotted, aging-ramp tokens untouched). Catalog `supply-strip` consumes it too, **deleting all 8 drifts**: hero `text-[22px] font-semibold`→`TileHero` (`text-data-lg` 20px, weight 600 retained from the token); `gap-4`→`gap-2`; the hand-rolled **rAF ease-out-quad** count-up (`1-(1-p)²`, off the single curve) → the shared `useCountUp`; `text-[11px]`→`text-micro` (label/Sub/HeroUnit/Mid/cta); literal `cubic-bezier(0.22,1,0.36,1)`→`var(--ease-smooth)` in HealthBar/Meter; tile padding `pt-16/pb-14`→`pt-14/pb-12`; header `mb-3`→`mb-1.5`. **Count-up correctness (load-bearing fix):** `useCountUp` = framer `animate()` on the one OPS curve, now referencing the **`EASE_SMOOTH` token** from `@/lib/utils/motion` (Jackson chose the token import over Books' inline `[0.22,1,0.36,1]` literal — single source of truth); reduced-motion → instant-final (correct alternative for a number reveal). Skills: `animation-studio:animation-architect` (+ auto `web-animations`) confirmed single-curve + reduced-motion; `custom-skills:interface-design` (extraction-fidelity lens); ops-copywriter n/a (zero label strings changed). **Done-gate `custom-skills:audit-design-system` over all 5 touched files — zero high findings** (no bare hex, no raw rgba, every color/size/font traces to a token; the one CRITICAL — easing literal where `EASE_SMOOTH` exists — fixed). `npx tsc --noEmit` clean for the 5 files (the 2 project tsc errors are a **sibling session's** in-flight deletion of `catalog/segment-toolbar.tsx` breaking its own `products-/stock-segment` imports — not mine, not imported by mine); `eslint` clean. **Live-verify GAP (environmental, not code):** Books pixel-identity established by DOM/class equivalence; visual screenshots on 3017 NOT captured — both dev servers had crashed under heavy parallel load (255s response times, 29s cold compiles), and after restarting `overhaul-shell` + bypass-auth the `/books` renderer intermittently hung at 30s and content didn't paint, while `/catalog` was concurrently **broken by the sibling's `segment-toolbar` deletion**. Live screenshots deferred until the environment settles. Atomic commit `8d060628` (5 files staged by name; sibling WIP untouched; no push). | Jackson (EASE_SMOOTH via token) / Session P4-1 |
| 2026-06-13 | **P4-1 remediation §5.5/§1 — A/R aging "current" color cross-surface split resolved** — the same A/R aging concept rendered in two color systems: Books AGING (`ar-aging-view.tsx`) colors **current = olive** (`bg-olive` #9DB582, on the canonical healthy→destructive ramp olive → tan → `fin-receivables` → rose → `fin-overdue`), while the Dashboard receivables widget colored **current = steel-blue**. **Finding correction (live-verified, supersedes §5.5's literal):** that steel-blue was **not** an off-palette `#417394` literal — it was `WT.accent` = `var(--color-ops-accent)`, the **runtime accent**, which `PreferencesApplier` overrides per-user from the curated accent palette. On the demo users the accent default *is* `#417394` (the audit eyedropped the rendered pixel and read that); a static `globals.css` read instead returns the `#6F94B0` default — both reconcile via the runtime override. So this was an **accent leak onto a data bar** (DESIGN.md §3: accent never on data bars), strictly worse than a stray literal — any user's chosen accent would color the bucket. **Fix:** new system-wide financial token `--fin-current` = olive `#9DB582` (= `--fin-profit`/`--olive`, the healthy/not-yet-due semantic), wired like the other `fin-*` tokens — design-system `colors_and_type.css` (no commit; ops-design-system is not a git repo) + app `globals.css` (`--color-financial-current`) + `tailwind.config.ts` (`financial.current` → `bg-financial-current`) + `WT.current`. The Dashboard `receivables-aging-widget` BUCKETS repointed at the **full canonical Books ramp** — not just current: `31-60` (was `WT.cost` rose) → `WT.receivables`, `61-90` (was `WT.errorMuted` brick@70%) → `WT.cost` rose, current → `WT.current` olive; `1-30`/`90+` already matched; `heroColor` fallback `?? WT.accent` → `?? WT.current`. The widget is now **accent-free**. **`#417394` retirement (§5.5c):** an exhaustive classification of all 74 `#417394` occurrences found **zero** financial/aging and **zero** internal status-dot uses — every one is a legitimate portal white-label default (20), a user color-picker palette entry (24, incl. the curated "Steel Blue"), or decorative chrome / anti-pattern docstring (26) — so there was nothing to retire from the financial/status palette (the audit's "drives Sent/active status dots" was the same accent mis-attribution; "Sent" = `WT.accent` too, part of the dashboard's accepted data-viz accent language across 23 widgets, which §6 calls "already cohesive" — out of scope, left untouched). DESIGN.md §3 financial table amended (`--fin-current` row + the one-ramp rule; file edit, no commit). **Skills:** `custom-skills:interface-design` (widget-change lens — "color encodes meaning, not the accent") + `animation-studio:data-visualization` (A/R ramp viz — confirmed the healthy→destructive ramp reads correctly); no motion authored (color-token swap; the existing bar height transition already uses the OPS curve), so `animation-architect` not gated; ops-copywriter n/a. **Done-gate `custom-skills:audit-design-system` over touched files — zero high findings** (all 6 WT tokens resolve to defined `--color-*` vars; the only hex added lives in token-definition sites `:root`/tailwind palette; component code is pure token refs). `npx tsc --noEmit` clean for the 4 files (the 1 project error — `estimates-segment.tsx` missing `FileText` — is pre-existing, neither mine nor the sibling's WIP); `eslint` clean (2 pre-existing unused-var warnings in the widget, untouched). **Live-verify (separate port, Jackson-chosen) — succeeded where the §5.1 shared-server attempt failed:** isolated detached worktree at the fix commit on **3019** with its own `.next` (no collision with the active sibling §5 session's 3017 server), dev-bypass as Pete on real Maverick data ($38,175 A/R / 14 invoices); Dashboard receivables **current = `rgb(157,181,130)` olive** (was the runtime accent), full ramp olive→tan→receivables→rose→brick, **identical** to Books AGING (`current` = same `rgb(157,181,130)`); both screenshots captured. **Parallel-session discipline:** a live sibling was mid-§5 pass on `globals.css`/`tailwind.config.ts` — my `globals.css` line was **hunk-staged** (`git apply --cached`) so their `.glass-subtle` removal stayed unstaged; their WIP never entered my index. Atomic commit `02dbb053` (4 files staged by name; no push). | Jackson (separate-port verify, commit now) / Session P4-1 |
