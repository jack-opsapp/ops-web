# Summary convergence repair

**Goal:** Resolve the seven reproduced deterministic summary failures and make nightly health distinguish availability, workflow impact and changes.

**Architecture:** Keep canonical source evidence, tenant identity, model trust checks and guarded snapshot writes. Align the commercial and current-fact next-action contracts. Normalize non-fact greetings/URLs consistently. Use the renderer's own typed clauses as evidence for schedule/action validation, preserving full strict validation for model output and monetary/scope checks.

**Required skills:** systematic-debugging, writing-plans, executing-plans, test-driven-development, verification-before-completion, Supabase. Design system: no UI changes.

1. Capture the seven live source snapshots read-only; reproduce all seven failures locally. Keep customer evidence in uncommitted artifacts only.
2. Add sanitized regression examples for contradictory next-action contexts, greeting/link-only actions, cross-field stale-action matches and historical schedule language in objections. Observe red before source changes.
3. Repair the smallest shared contract boundaries. Run the existing 82 renderer tests, trust-boundary and targeted/scheduled writer tests. Confirm all seven original snapshots converge without a model call.
4. Verify current Phase C retry behavior and preserve truthful failure handling; do not acknowledge an unwritten summary, weaken identity/concurrency checks or synthesize a business action.
5. Run focused suites, TypeScript, lint, diff checks and production build as applicable; compare inherited failures to unchanged production. Commit task-owned files, safely merge locally and independently verify.
6. Update the Bible and canonical bug with source/local proof; deployment and natural production receipt remain explicit. Do not rewrite customer records or replay business work.
7. Save and read back the automation reporting contract, preserving its schedule, model and authority boundaries. Record the final result and current run time in automation memory.

## Verification result

- All seven exact source snapshots failed before the repair and pass afterward without a model call or customer-data write. Private source fixtures are uncommitted verification artifacts.
- Five sanitized reproductions failed before source changes. The repaired renderer, trust boundary, targeted/scheduled writer, continuation and Phase C suites pass 400 committed tests; six route/RPC guard suites pass another 39. The temporary seven-record reproduction also passes.
- Full production-source TypeScript passes. The all-files compiler reports three unrelated MCP test errors; compare against the untouched production baseline in the final artifact. ESLint reports zero errors and eight existing warnings. Formatting and diff checks pass.
- Phase C still requires exactly one successful guarded snapshot write. Its retry path does not use the older mailbox-continuation three-attempt cap. No quarantine, worker-acknowledgement, queue, customer-record, migration or provider behavior is changed by this source repair.
- The saved nightly prompt now reports service availability and background workflows separately, compares exact affected records/events, and keeps unchanged issues brief. Schedule, model and production authority are unchanged.
- Deployment remains a separate explicit approval and must be followed by natural exact-event completion proof. Do not mark the canonical bug resolved from local tests alone.
