/**
 * Exact correspondence content is retained without truncation up to this
 * UTF-8 byte bound. Provider capture and normalization must share this value.
 */
export const MAX_EXACT_CORRESPONDENCE_CONTENT_BYTES = 8 * 1024 * 1024;
